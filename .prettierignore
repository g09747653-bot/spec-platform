node_modules/
.next/
coverage/
playwright-report/
test-results/
pnpm-lock.yaml
next-env.d.ts

# Next.js rewrites and reformats this file on every dev/build run; it owns the
# formatting, so Prettier must not fight it.
tsconfig.json

# Generated SQL — Drizzle owns the formatting.
migrations/

# The specification bundle and its operating instructions are hand-authored
# source of truth; a formatter must never rewrite them.
.specs/
CLAUDE.md

# Gate artifacts are evidence, not source: the bundle files are what the models actually
# wrote and the report is what the walk actually recorded. A formatter must not rewrite
# either — a reformatted artifact is no longer the artifact.
artifacts/

# Golden fixtures of the machine export (task 150): the markdown inputs are live documents
# fixed byte-for-byte, and the JSON outputs are pinned derivations compared as bytes in tests.
# A reformatted golden is no longer golden.
fixtures/spec-bundle/golden/

# Vendored methodology templates and the module generated from them (task 116).
# These are copies of somebody else's licensed files: reformatting one makes it no longer
# the file the LICENSE beside it covers. The generated module is regenerated by
# `node scripts/build-methodology-templates.mjs`, which owns its formatting.
src/modules/methodologies/templates/
