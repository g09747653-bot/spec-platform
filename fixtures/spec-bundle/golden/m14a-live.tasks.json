{
  "bundleId": "m14a-live-bundle",
  "projectId": "m14a-live-project",
  "tasks": [
    {
      "taskId": "1.1",
      "title": "Определение и разрешение путей рабочей директории",
      "description": "* *Описание:* Реализовать логику определения пути рабочей директории на основе переменной окружения `TODO_WORK_DIR` или системных путей по умолчанию (`~/.config/todo/` для Linux/macOS и `%APPDATA%/todo/` для Windows).\n  * *Критерии приемки:* Путь корректно преобразуется в абсолютный. Корректно обрабатывается символ `~` в UNIX-системах.",
      "dependsOn": [],
      "metadata": {
        "expectedArtifacts": []
      }
    },
    {
      "taskId": "1.2",
      "title": "Создание структуры папок и разграничение прав доступа",
      "description": "* *Описание:* Реализовать автоматическое создание рабочей директории и поддиректории `backups`. На UNIX-подобных системах применить системные вызовы для установки прав доступа `0700` (`chmod 700`) для создаваемых директорий.\n  * *Критерии приемки:* Папки создаются автоматически при их отсутствии. В Linux/macOS права доступа строго соответствуют `0700`.",
      "dependsOn": [],
      "metadata": {
        "expectedArtifacts": []
      }
    },
    {
      "taskId": "2.1",
      "title": "Инициализация базы данных и схемы (clean_slate)",
      "description": "* *Описание:* Реализовать проверку наличия файла базы данных `todo.db` в вычисленной рабочей директории. Если файл отсутствует, он должен создаваться автоматически с правами `0600` на POSIX-системах. При создании файла должна разворачиваться схема таблицы `tasks` и создаваться индексы `idx_tasks_due_date` на поле `due_date` и `idx_tasks_is_completed` на поле `is_completed`. Инициализация выполняется с чистого листа без использования исторических миграций.\n  * *Критерии приемки:* Файл `todo.db` создается автоматически строго внутри вычисленной `<WORK_DIR>`. Схема содержит таблицу `tasks` со столбцами: `id` (INTEGER PRIMARY KEY AUTOINCREMENT), `title` (TEXT NOT NULL), `due_date` (TEXT NOT NULL), `is_completed` (INTEGER NOT NULL DEFAULT 0), `created_at` (TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP). Индексы успешно созданы. На POSIX права файла равны `0600`.",
      "dependsOn": [],
      "metadata": {
        "expectedArtifacts": []
      }
    },
    {
      "taskId": "2.2",
      "title": "Реализация репозитория задач (TaskRepository)",
      "description": "* *Описание:* Написать класс репозитория для работы с SQLite. Все запросы на чтение и запись должны выполняться с использованием параметризованных запросов (placeholders `?`) во избежание SQL-инъекций. Реализовать методы: `insert_task(title, due_date)`, `get_task_by_id(task_id)`, `update_task_status(task_id, is_completed)`, `fetch_active_tasks(local_today)`, `fetch_all_tasks(local_today)`.\n  * *Критерии приемки:* Ни один запрос не использует прямую склейку строк. Операции изменения данных (`insert`, `update`) выполняются строго в рамках транзакций.",
      "dependsOn": [],
      "metadata": {
        "expectedArtifacts": []
      }
    },
    {
      "taskId": "3.1",
      "title": "Механизм автоматического бэкапа перед транзакциями записи",
      "description": "* *Описание:* Реализовать модуль `BackupService`, копирующий рабочий файл `todo.db` в папку `backups` перед совершением любых транзакций записи (добавление новой задачи, изменение статуса существующей задачи). Имя файла должно соответствовать шаблону `todo.db.bak.YYYYMMDDHHMMSSffffff`, где временная метка вычисляется по UTC с точностью до микросекунд.\n  * *Критерии приемки:* Копия файла успешно создается до момента физической записи в рабочий файл БД. В UNIX-системах копия создается с правами `0600`. При ошибке создания бэкапа (например, нет места на диске) текущая транзакция записи прерывается, выводится сообщение в `stderr`, утилита завершает работу с кодом `1`.",
      "dependsOn": [],
      "metadata": {
        "expectedArtifacts": []
      }
    },
    {
      "taskId": "3.2",
      "title": "Алгоритм ротации резервных копий",
      "description": "* *Описание:* Реализовать логику контроля количества бэкапов. После успешного создания новой копии сервис должен проверить общее количество бэкап-файлов в папке `backups`. Если их больше 5, файлы сортируются лексикографически по имени, и самый старый файл удаляется.\n  * *Критерии приемки:* В папке `backups` всегда сохраняется ровно 5 последних файлов резервных копий. Алгоритм ротации корректно отрабатывает при высокой частоте вызовов.",
      "dependsOn": [],
      "metadata": {
        "expectedArtifacts": []
      }
    },
    {
      "taskId": "4.1",
      "title": "Валидация входных данных бизнес-слоя",
      "description": "* *Описание:* Реализовать валидацию заголовка задачи (не пустой, содержит не только пробелы) и даты (формат `YYYY-MM-DD`, реальная календарная дата в диапазоне лет 1900–2100).\n  * *Критерии приемки:* Ошибки валидации вызывают исключения бизнес-логики для последующей обработки на уровне представления.",
      "dependsOn": [],
      "metadata": {
        "expectedArtifacts": []
      }
    },
    {
      "taskId": "4.2",
      "title": "Логика фильтрации задач на сегодня (overdue_included / auto_include)",
      "description": "* *Описание:* На стороне приложения вычислить текущую локальную системную дату в формате `YYYY-MM-DD` (без использования функций времени SQLite вроде `CURRENT_DATE`, чтобы избежать смещения поясов). Реализовать выборку задач согласно следующим правилам:\n    * *Без флага `--all`:* выбираются только невыполненные задачи (`is_completed = 0`), у которых `due_date <= local_today` (что включает просроченные задачи из прошлого).\n    * *С флагом `--all`:* выбираются невыполненные задачи (`is_completed = 0`), у которых `due_date <= local_today`, а также выполненные задачи (`is_completed = 1`), у которых `due_date = local_today`.\n  * *Критерии приемки:* Логика фильтрации однозначно исключает будущие невыполненные задачи при запросе списка задач. Выборка содержит просроченные дела из прошлого. Сортировка записей в выборке: по `due_date` (ASC), затем по `id` (ASC).",
      "dependsOn": [],
      "metadata": {
        "expectedArtifacts": []
      }
    },
    {
      "taskId": "4.3",
      "title": "Идемпотентность закрытия задач (noop для todo done)",
      "description": "* *Описание:* Перед выполнением отметки задачи как выполненной проверить ее текущий статус в БД. Если задача уже выполнена (`is_completed = 1`), то операция записи не инициируется, бэкап не создается, транзакция записи не открывается. Происходит успешный холостой возврат (noop).\n  * *Критерии приемки:* Повторный вызов `todo done <id>` завершается успешно (код `0`) мгновенно, не плодя новые файлы бэкапов и не перезаписывая диск.",
      "dependsOn": [],
      "metadata": {
        "expectedArtifacts": []
      }
    },
    {
      "taskId": "5.1",
      "title": "Разбор аргументов командной строки (CLI Parser)",
      "description": "* *Описание:* С помощью стандартного модуля `argparse` реализовать парсинг трех команд: `add`, `list`, `done`. Настроить поведение параметра `--date` для команды `add`: если параметр `--date` опущен в CLI, в качестве `due_date` в базу данных должна записываться текущая локальная системная дата в формате `YYYY-MM-DD`.\n  * *Критерии приемки:* Синтаксические ошибки разбора (неизвестный флаг, отсутствие обязательных параметров) приводят к выводу справки/сообщения в `stderr` и завершению работы с кодом `2`. Нечисловые или неположительные идентификаторы в команде `done` вызывают ошибку синтаксиса (код `2`). В БД пишется корректная дата по умолчанию при отсутствии явного указания `--date` в команде `add`.",
      "dependsOn": [],
      "metadata": {
        "expectedArtifacts": []
      }
    },
    {
      "taskId": "5.2",
      "title": "Форматирование вывода в stdout",
      "description": "* *Описание:* Настроить вывод результатов выполнения команд:\n    * При успешном выполнении `todo add`: выводится только ID созданной задачи.\n    * При успешном выполнении `todo done`: вывод полностью отсутствует (тишина).\n    * При выполнении `todo list`: выводится список строк без заголовков столбцов и пустых строк. Поля разделяются символом табуляции `\\t`: `<id>\\t<статус>\\t<дата>\\t<название>`. Строгое представление статусов: `[ ]` для невыполненных, `[X]` для выполненных.\n  * *Критерии приемки:* Формат вывода строго соответствует спецификации, не содержит декоративных элементов и псевдографики.",
      "dependsOn": [],
      "metadata": {
        "expectedArtifacts": []
      }
    },
    {
      "taskId": "5.3",
      "title": "Централизованная обработка ошибок и кодов возврата (Exit Codes)",
      "description": "* *Описание:* Реализовать перехват всех системных и семантических исключений в точке входа приложения. Перехватывать ошибки отсутствия прав доступа, неверного формата даты, несуществующего ID задачи в `todo done`. Выводить лаконичное сообщение на русском языке в поток `stderr` (без отображения stack trace) и завершать работу с кодом `1`.\n  * *Критерии приемки:* Исключены падения программы с необработанными трейсбеками Python. Любая штатная логическая или дисковая ошибка завершается с кодом `1`.",
      "dependsOn": [],
      "metadata": {
        "expectedArtifacts": []
      }
    },
    {
      "taskId": "5.4",
      "title": "Реализация опционального отладочного логирования (TODO_DEBUG=1)",
      "description": "* *Описание:* Если в окружении установлена переменная `TODO_DEBUG=1`, приложение должно записывать техническую информацию, выполняемые SQL-запросы и stack trace возникающих ошибок в файл `todo.log`, располагаемый непосредственно в вычисленной рабочей директории приложения. По умолчанию логирование в файл отключено. Чтобы предотвратить неконтролируемое разрастание файла, использовать ротируемый обработчик логов с ограничением размера.\n  * *Критерии приемки:* При `TODO_DEBUG=1` файл `todo.log` создается в рабочей директории и наполняется отладочной информацией. В штатном режиме (переменная не задана) лог-файл не создается и не изменяется.",
      "dependsOn": [],
      "metadata": {
        "expectedArtifacts": []
      }
    },
    {
      "taskId": "6.1",
      "title": "Модульные тесты (Unit Tests)",
      "description": "* *Описание:* Написать тесты для парсера аргументов (включая проверку значения по умолчанию для даты при добавлении задачи), валидатора дат и правил фильтрации задач.",
      "dependsOn": [],
      "metadata": {
        "expectedArtifacts": []
      }
    },
    {
      "taskId": "6.2",
      "title": "Интеграционные тесты (Integration Tests)",
      "description": "* *Описание:* Написать тесты для проверки инициализации БД (`clean_slate`), транзакционности записи, механизма создания и ротации бэкапов (включая проверку лексикографической ротации по временным меткам), а также прав доступа к создаваемым файлам.",
      "dependsOn": [],
      "metadata": {
        "expectedArtifacts": []
      }
    },
    {
      "taskId": "6.3",
      "title": "Системные E2E-тесты (CLI Tests)",
      "description": "* *Описание:* Реализовать автоматизированный запуск утилиты в изолированном окружении, проверку кодов возврата процесса и сопоставление вывода в `stdout`/`stderr` с эталонными шаблонами.\n\n---",
      "dependsOn": [],
      "metadata": {
        "expectedArtifacts": []
      }
    }
  ]
}
