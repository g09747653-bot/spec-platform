# START_HERE — точка входа исполнителя

> Пишет Архитектор. Фраза заказчика «Прочитай .specs/handoff/START_HERE.md и исполни» = вердикт ниже утверждён, включая приёмку заказчика глазами.

**Сессия:** Программа А · шаг 0 — dogfooding: наш продукт автономно генерирует ТЗ собственного следующего этапа · **Дата:** 2026-08-19 · **Режим сессии: MAX** (ultracode не нужен: думает продукт, а не ты; твоя работа — прогон, фиксация, рапорт)

## Вердикт по раунду 3: 144, 145 и 146 ПРИНЯТЫ (А-19). M13п закрыт

Это была лучшая работа программы, и принята она без оговорок. Что зачтено поимённо: **ход выбирает код, содержание — модель** — одновременно ответ на P1 и структурная защита от инъекции (пять опровергнутых находок это доказали, а не задекларировали); **два стража, найденные зондом**, — свойство, которое не читается с кода, проверено удержанием вызова модели; **режим как строка прогона** с CHECK-эквивалентностями; тест тождества БД, который сначала измеряет эталонную прогулку; и честность артефактов — RESULT-autonomous сохраняет и «шагов 0» у панели, и первый неудачный запуск прогулки А. Правка правила второго лица с повторным замером 3/3 — образец того, как AC становится измеренной. D-227…D-238 приняты.

Ответы на §9: **(2)** три строки внесены в solution.md §API (сверено с кодом маршрута), `AUTONOMOUS_RUNS` внесена в ERD; заодно закрыто моё же упущение — `SESSION_MESSAGES` из А-13 в ERD не была внесена, теперь стоит. **(3)** P2 — твоё прочтение утверждено дословно: делегированное решение, а не отсутствующее; включение драйвера — явное, отзывное, на одну сессию; автор каждого решения назван в ленте. **(4)** Собственный бюджет драйвера — НЕ вводить: узкий бюджет делает автономный документ хуже ручного (против «не хуже»), а измеренная стоимость сидит в несходимости ревью на больших документах (45→42→42→47→45), которую он прятал бы, а не лечил. Кандидат «сходимость-осознанная остановка» записан в Backlog — решать по замерам Программы А. **(6)** Ограничение локального регистра принято: гейты Программы А про качество контента — на облачном звене.

## Порядок шага 0

1. **Теги первым действием** (на смерженном main): `m13p-accepted`; проверь `git tag -l` — если `m12p-accepted` ещё не стоит, поставь и его той же командой (приёмка M12п состоялась, тег мог остаться неповешенным на шве сессий).
2. **Автономный dogfooding-прогон.** Профиль гейта (свежий ключ Google спереди, `ollama serve` окно 16384 запасным), черновая база, русский интерфейс. Новая сессия: MySpec greenfield · профиль «технический» · стиль «Конкретный» · язык содержания следует seed (русский). Seed — дословно из §Seed ниже, галочка «Пусть ИИ проведёт этот чат сам», и дальше ни одного клика — тот же харнесс, что прогулка Б гейта M13 (счёт кликов обязан держаться).
3. **Фиксация бандла.** Экспортируй ZIP и положи содержимое побайтово в `.specs/research/programma-a/` (constitution.md, requirements.md, solution.md, tasks.md) + рядом `RESULT-A0.md` (машинный отчёт прогона: шаги, ревизии, доски, записи драйвера с обоснованиями, packing-строки, консоль) и скрин финального состояния. Это сырьё моего следующего шага: я читаю бандл и нарезаю из него план Программы А — не ты.
4. **Рапорт** — новая секция сверху в `.specs/handoff/REPORT-A0.md` (новый файл, той же формой), в чат — две строки.

## Именованные поведения и границы

- **Честная остановка драйвера — находка, а не провал.** Если прогон остановился именованной причиной — зафиксируй позицию, причину и ленту в рапорте. Если причина — свойство seed (например `seed-too-thin`), разрешаю ровно одну правку seed с сохранением всего содержания (D-строка с формулировкой до/после); вторая остановка = стоп и вопрос мне. Ничего не «доигрывай» руками — ручное завершение обесценило бы сырьё.
- **Ни строки продуктового кода.** Это прогон, а не milestone: никаких правок src/**, никакого Backlog «заодно» (B-1…B-4 лежат, где лежат). Допустим только новый live-скрипт `e2e/dogfood-A0.live.ts` по образцу прогулки Б.
- **Реализация Программы А не начинается.** План из бандла делаю я амендментом к tasks.md после чтения; твоя сессия заканчивается рапортом.
- Прежний перечень «не трогать» в силе целиком (state-machine, А-8/А-10, heartbeat, Р-контракты, бюджеты, парити-якорь, побайтовый экспорт, immutability, реестр строк).

## Seed (вставить дословно, одним сообщением)

Автономный контур доставки: система, которая ведёт короткую задумку — текст или голос — до готового протестированного продукта без участия человека. Вход контура — готовый спецификационный бандл из Spec Platform (конституция, требования, архитектура, задачи). Дальше работают агенты на машине владельца: агент-архитектор читает бандл, нарезает работу на milestone-сессии и пишет файловые handoff-задания; агенты-исполнители (Claude Code) выполняют по одному milestone за сессию, сами тестируют каждый гейт (юниты, e2e, живые прогулки с артефактами) и пишут рапорты; архитектор проверяет артефакты и принимает или возвращает на доработку. Человек может в любой момент наблюдать ленту, остановить контур и забрать управление. Первая обязательная часть — локальный однопользовательский режим самой Spec Platform: запуск всего стека на машине владельца без облака и без OAuth (авто-сессия владельца, локальная база, локальная цепочка моделей, облачные ключи по выбору). Вторая — кодификация handoff-протокола как исполняемого конвейера: роли, гейты приёмки, уровни решений, BLOCKED-протокол, красный CI как полная остановка. Третья — параллельность: до десяти исполнителей под одним архитектором, с контроллерами и исследователями, для проектов уровня игры на движке. Телеграм-вход (короткая задумка текстом или голосом) подключается последним, когда контур уже доказан. Ограничения: одна машина Windows с 16 ГБ VRAM; протокол файловый и переживает обрыв любой сессии; каждое действие агентов оставляет читаемый след.
