# START_HERE — точка входа исполнителя

> Пишет Архитектор. Фраза заказчика «Прочитай .specs/handoff/START_HERE.md и исполни» = вердикт ниже утверждён.

**Раунд:** M5 · раунд 1 · **Дата:** 2026-08-13 · **Режим сессии: Opus high** · **Сессия: новая**

## Гейт M4: ПРИНЯТ

Проверено по коду: порядок слоёв резолвера — abstain-гейты до матчеров, глагольный гейт перед моделью, фильтр по действиям карточки. 10/10 чекбоксов, 1047 unit, D-54 записан. D-55…D-63 приняты без замечаний. Обе находки кластера (момент рождения ревью; тест-даблы в маршрутах и потеря позиции при отказе ревью) — именно тот класс дефектов, ради которого гейты существуют; зачтено. Уборка после упавшей сессии проведена правильно.

Живой смоук промптов M4 (`review.board.v1`, `refinement.propose.v1`, `decision.intent.v1`) — решением Архитектора перенесён на гейт M6: полный ручной проход MVP на реальном провайдере покроет их естественно.

**Первым действием:** `git tag m4-accepted <текущий зелёный main> && git push origin m4-accepted`.

## Задание: Milestone 5 — Attachments, Parsing & Research (задачи 63–71)

Цель: пользовательские документы и живой веб-поиск заземляют генерацию — безопасно.

### Ключи

Заказчик кладёт `env.m5.txt` (BLOB_READ_WRITE_TOKEN, WEB_SEARCH_API_KEY). Обычный ритуал: перенести в `.env`, удалить файл, проверить гитигнор и историю. В Vercel: Blob-токен появляется автоматически при подключении стора к проекту — **проверь его наличие в обоих окружениях**; `WEB_SEARCH_API_KEY` заказчик добавляет руками (Production + Preview). Урок M1 в силе: перевод ключей в обязательные (по правилу D-46/D-8 — для активных интеграций) не мержить, пока переменных нет в Vercel.

### Акценты Архитектора

- **64 — отказ до первого байта в стор** (SC-9): негативные тесты обязаны утверждать, что при отказе Blob-запись не вызывалась вовсе, а не «вызвалась и откатилась».
- **65 — извлечение один раз при загрузке** (DR-8), таймаут на извлечение, sniffed MIME решает, а не заявленный; незарегистрированный тип не доходит до экстрактора.
- **69 — `context_attachment_ids` на каждой ревизии** (DR-12): late-attachment анализ считается из персистентного состояния; никакой автоправки одобренных файлов. Задача Large — дай ей соответствующее внимание.
- **70 — research fail-open** (FR-019 AC-4): отказ поиска никогда не валит стадию; байт-кап до модели; событие `research` в протоколе стрима уже существует с M3 — подключи, не изобретай новое. **Мини-смоук живьём:** один настоящий search+fetch через ключ заказчика (вне CI), результат в рапорт — дешёвая проверка, что ключ и адаптер живые.
- **71 — untrusted-блоки** — фундамент для инъекционного корпуса M8: вложения и веб-контент входят в промпт только внутри размеченных блоков данных; гейты к этому контенту слепы by construction.
- Backlog-строка «thinkingBudget=0 для интервью-раундов» (D-54 п. 2): если в этом milestone коснёшься агентских вызовов — исполни; нет — оставь до первого касания.

### Рамки

PR-порядок прежний (D-19). Объём — строго задачи 63–71. Рапорт: `.specs/handoff/REPORT-M5.md`, в чат — две строки. **M6 не начинай.** После M5 остаётся один milestone до MVP-черты — гейт M6 будет главным: полный ручной проход заказчика на реальном провайдере.
