# START_HERE — точка входа исполнителя

> Пишет Архитектор. Фраза заказчика «Прочитай .specs/handoff/START_HERE.md и исполни» = вердикт ниже утверждён.

**Раунд:** M2 · раунд 1 · **Дата:** 2026-08-12 · **Режим сессии: Opus MAX** · **Сессия: новая**

## Гейт M1: ПРИНЯТ

Код принят на раунде 1; мерж-раунд проверен: `main` = `f75fd4e`, CI и прод-деплой зелёные, тег `m1-accepted`, ветки прибраны, триггеры задачи 16 подтверждены в прод-БД. Ручной проход заказчика: живой вход через Google, полный цикл промпт → генерация → правка → одобрение → экспорт. Инцидент PR #7→#8 разрешён корректно; исправление про `created_at` в журнале миграций принято и учтено — при будущих проверках ни ты, ни я не используем этот столбец как время применения.

## Задание: Milestone 2 — Workflow Engine & Interview (задачи 24–38)

Цель: заменить неявный поток скелета настоящей, исчерпывающе протестированной машиной состояний и структурированным интервью. Это **ядро корректности всего проекта** — единственный milestone, который я просил вести в максимальном режиме с первого подхода.

### Кластер 24–30 — сердце milestone

- Серийный по природе (tasks.md, Risks): не дробить, не перемежать с 31–38.
- Таблица переходов — плоский перечислимый массив; каждый гейт — чистый предикат над снапшотом, ноль I/O (NFR-012 AC-1). Все нелегальные переходы отклоняются типизированной причиной.
- Задача 30 — «highest-leverage test in the plan»: программное перечисление ВСЕЙ матрицы, каждое ребро минимум с одним проходящим и одним отказывающим случаем, каждый `ReasonCode` заасserчен, оба порядка Quality, цикл `complete → quality → complete`. Помни правило плана: **провал в этой матрице — дефект дизайна, а не теста.** Если матрица сопротивляется — это сигнал остановиться и думать, а не подгонять тест.
- `workflow_state` уже существует со времён M1 (CHECK-констрейнты из общих кортежей, D-15) — расширяй, не пересоздавай; следи, чтобы таблица переходов и DDL остались производными одного источника.

### Задачи 31–38 — интервью

MCQ-карты со схемой (2–8 опций, обязательный free-text выход), бюджет раундов как гейт (`ROUND_LIMIT_REACHED`), информационные потребности по ключам, фолбэк исчерпания, summary + подключение interview-гейта к реальному transition-эндпоинту. Всё на стаб-провайдере — LLM-ключи в этом milestone не нужны и не запрашиваются.

### Рамки

- PR-порядок по execution.md §2 (D-19): зелёные чеки, squash, ветки удалять, `main` всегда зелёный. Разумная гранулярность: кластер 24–30 может быть одним PR, интервью — одним-двумя.
- Идеи «заодно» — в Backlog. Объём — строго задачи 24–38.
- Рапорт: `.specs/handoff/REPORT-M2.md`, форма прежняя, раунды сверху. В чат — две строки.

**M3 не начинай.** Гейт M2 — ключевой: я буду выборочно перепроверять матрицу переходов против requirements.md независимо от твоих тестов.
