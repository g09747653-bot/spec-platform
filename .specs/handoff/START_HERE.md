# START_HERE — точка входа исполнителя

> Пишет Архитектор. Фраза заказчика «Прочитай .specs/handoff/START_HERE.md и исполни» = вердикт ниже утверждён.

**Сессия:** M10п · раунд 2 — задачи 131, 128, 129 (финал этапа 1) · **Дата:** 2026-08-17 · **Режим сессии: Opus high**; на задаче 128 — один ultracode red-team прогон

## Вердикт по раунду 1: принят. Все три вопроса §7 решены (амендмент А-11 внесён)

Раунд сильный: хроматическое расстояние от их бренда как тест, а не обещание (D-163); контраст, измеренный конверсией OKLCH→sRGB по 25 парам; и главное — D-167: лоадер, тихо превративший 404 чужой сессии в мягкий 200, пойман двумя красными тестами и починен по документации. Это дефект класса «дожил бы до публичного доступа» — ровно то, ради чего каждая задача несёт тесты.

**Ответы §7:**

1. **D-166 — подтверждаю: баннер, не модалка.** Д-1 сильнее визуального парити: модалка отнимала бы Stop у идущей генерации. В чек-лист 128 — как «осознанное отличие своего оттенка», с формулировкой «здесь мы сильнее оригинала».
2. **D-171 — подтверждаю: эндпоинт `revert` добавлен в API-таблицу solution.md (А-11).** Альтернатива с миграцией отвергнута. Твоё рассуждение «источник — факт, а не утверждение клиента» записано в амендмент дословно.
3. **D-165 — принято как названное свойство.** Мгновенный баннер при неудавшемся session-request — честное следствие контракта перечитывания; отдельной задачи нет.

## Объём: закрыть M10п (131 → 128 → 129)

1. **131 (А-10):** грамматически-ограниченный JSON у локального адаптера (Ollama `format` + схема) для Edit-предложения, review.v2 и черновиков интервью; облачный снапшот не меняется; строка в лог от `atMostOneRecommended` с тестом; предполёт обязан показать three-for-three на состоянии D-161. Не тянет под ограничением — именованное ограничение локального режима, не молчаливый уход в облако.
2. **128 (чек-лист + red-team):** строится из эталона §1.1–1.5, вердикт каждому пункту: парити / осознанное отличие (D-166 уже в этом списке) / разрыв. Разрывы не самоамнистируются — чинить в 129 или нести мне. Затем один ultracode red-team прогон, адверсарно ищущий пропущенные расхождения; его находки — все с диспозицией. Артефакты `artifacts/parity-M10/`.
3. **129 (гейт):** профиль — свежий ключ спереди, предполёт `qwen3:8b` (структура + JSON-раунд + ограниченный Edit; проходит — берёшь её, нет — `qwen3:14b`); полная прогулка на финальном визуале, обе темы смоуком; ноль обрезок, ноль структурных отказов; артефакты `artifacts/gate-M10/`. Зелёный гейт → рапорт; теги `m10p-accepted` и `stage1-complete` — только после моей приёмки.

## Не трогать

А-8, heartbeat/Р-4, Р-1/Р-2/Р-3, D-99…D-101, парити-якорь, побайтовый экспорт, immutability ревизий, токен-гейт цветов (сам себя не ослаблять). Рапорты — «Раунд 2» сверху в REPORT-M10.md, в чат — две строки. **Программу А не начинать.**
