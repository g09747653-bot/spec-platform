# START_HERE — точка входа исполнителя

> Пишет Архитектор. Фраза заказчика «Прочитай .specs/handoff/START_HERE.md и исполни» = вердикт ниже утверждён.

**Сессия:** M9п · Методологии и IA (задачи 116–123) · **Дата:** 2026-08-15 · **Режим сессии: Opus high** · Milestone может занять две сессии — шов после задачи 119

## Вердикт по M8п: ПРИНЯТ

Проверено по коду, артефактам и PR #39. Отдельно отмечаю три вещи. Разбор гидрационного предупреждения — образец диагностики: причина найдена в инструменте (каретка Playwright), продукт не тронут, кандидат из Backlog честно опровергнут (D-110). Отказ менять transition-table при формулировке задачи «extended for the loop» — правильное прочтение: бюджет на решении `request_changes`, обратное ребро безусловно (FR-007 AC-5), формулировку писал я, прочтение твоё точнее — зачёт (D-113). Фильтр «неотмеченные пункты отсутствуют в промпте вовсе» — гарантия по построению, а не по просьбе (D-114) — ровно так и надо.

**А-5 внесён Архитектором** (обе твои поправки из §6): `REVISION_LIMIT_REACHED` — в закрытом наборе `ReasonCode` и таблице ошибок solution.md; схема ревью в solution.md приведена к review.v2 (verdict/summary/mustFix/recommendations, confidence 1..10, `line` исключён сознательно). Код не трогать — документ приведён к твоей реализации.

**Решение по открытому вопросу §2:** отдельный живой прогон линтеров не нужен. Доказательная база задачи 114 (маршрутный тест через настоящий эндпоинт + 18 фикстур) достаточна; живое свидетельство добирается бесплатно в гейте M9п — его маршруты проходят requirements/tasks, и RESULT фиксирует счётчик линтерных пунктов на каждой такой доске (вписано в задачу 123).

## Объём сессии: M9п (116 → 123 по порядку)

Декомпозиция уточнена мной на этом гейте — читай блок «Milestone 9п» в tasks.md целиком, там полные AC. Суть: методологии становятся данными, поверхность — информационной архитектурой продукта. Эталон: §1.4 (пять графов), §1.5 (сайдбар, проектная страница, композер), §5.1 (viewer, Edit-флоу), §5.2 (первоисточники шаблонов).

Особые указания Архитектора:

1. **116 — фундамент milestone.** Дефолтный конфиг обязан воспроизводить текущую 33-рёберную матрицу байт-в-байт (снапшот-тест) — это твой якорь, что рефакторинг ничего не сдвинул. Шаблоны SpecKit/OpenSpec — вендоринг из первоисточников с LICENSE и NOTICE, не пересказ по памяти; их текст идёт в модель только через единую точку сборки (У-1 действует для каждой методологии).
2. **117 — контракт экспорта M6 неприкосновенен для дефолтной методологии:** ровно 4 файла с точными именами, существующий export-e2e не трогается. Чужие наборы файлов — из конфига. Auto-выбор: одна дешёвая классификация, безопасный дефолт при любом сбое, имени вендора пользователь не видит.
3. **118 — Edit:** атомарность мультифайлового применения (транзакция, тест с индуцированным сбоем посредине); reject = байт-в-байт (контракт M4 переутверждается здесь).
4. **119/120 — заглушки честные:** Mount folder и MCP-карточка не делают ни одного сетевого вызова и не обещают того, чего нет. Архивация обратима и ничего не удаляет.
5. **121 — слэш-команды и @-ссылки не создают новых write-path:** те же эндпоинты, что кнопки (тест «одно и то же состояние БД» по образцу M4); модели без ключей скрыты честно; Auto = существующая failover-цепочка.
6. **Не трогать:** Р-1/Р-2/Р-3, D-99…D-101, проекцию ленты (новые поверхности — расширением проекции), M8п-цикл, решённые доски.
7. **Гейт 123 — живой набор ограничен сознательно** (квота): полный путь SpecKit-greenfield + короткий brownfield + живой Edit поверх полученного бандла + живая смена модели; все пять конфигов — на стаб-e2e; счётчики линтеров на requirements/tasks досках — в RESULT. Артефакты `artifacts/gate-M9/`, лёгкий trace, снимки с `caret: 'initial'` (урок D-110).
8. **Шов сессии:** если после задачи 119 контекст на исходе — остановись, рапорт «раунд 1» в REPORT-M9.md, продолжение новой сессией с этого же START_HERE (заказчику — та же фраза).

## Стоп-правила

Прежние: красный CI = стоп; 2 подхода → BLOCKED; идеи «заодно» → Backlog. **M10п не начинай** (его декомпозицию уточню на этом гейте). Рапорт — новой секцией сверху в `.specs/handoff/REPORT-M9.md` (новый файл), в чат заказчику — две строки по протоколу.
