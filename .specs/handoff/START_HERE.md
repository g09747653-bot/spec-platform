# START_HERE — точка входа исполнителя

> Пишет Архитектор. Фраза заказчика «Прочитай .specs/handoff/START_HERE.md и исполни» = вердикт ниже утверждён.

**Сессия:** M15а · раунд 2 — подписочные учётки исполнителя и живой прогон гейта 158 · **Дата:** 2026-08-20 · **Режим сессии: MAX**

## Что изменилось со вчерашнего вердикта (А-23)

Заказчик отверг API-ключ по стоимости — довод принят, контур ведём на его подписке Claude Code. Прежний §1 раунда 2 (про `ANTHROPIC_API_KEY` в `env.loop.txt`) заменён этим документом; всё остальное из А-22 в силе: 152–157/168/169 приняты, задачи 170/171 стоят в M16а, улики в `.local/` хранятся по прежнему правилу.

## Порядок раунда 2

1. **Проверка подписочного пути — документацией и зондом, в этом порядке.** Вопросы, на которые нужен измеренный ответ: (а) читает ли неинтерактивный запуск (`-p`) переменную `CLAUDE_CODE_OAUTH_TOKEN` (долгоживущий токен `claude setup-token`); (б) совместима ли она с `--bare`; (в) если `--bare` с подписочным токеном несовместим — какой минимальный набор флагов даёт ту же изоляцию: без хуков, без MCP, без `CLAUDE.md`/настроек редактируемого репозитория. Канал инъекции, который закрывал `--bare` (D-255), обязан остаться закрытым — и в рапорте названо, чем именно он закрыт теперь. Сначала зонд на хосте, затем тот же зонд в контейнере.
2. **Конфиг `loop/`:** обязательное поле становится «ровно один из `ANTHROPIC_API_KEY` | `CLAUDE_CODE_OAUTH_TOKEN`» (валидация, тексты ошибок, тесты); обёртка передаёт в контейнер ту переменную, которая есть, — точечно, как и раньше.
3. **Токен заказчика.** Заказчик выполняет `claude setup-token` у себя и кладёт `env.loop.txt` в корень проекта со строкой `CLAUDE_CODE_OAUTH_TOKEN=…`. Твой шаг: перенести в `loop/.env`, удалить файл, убедиться, что git не видит ни того ни другого. Значение не печатать нигде, даже частично.
4. **Живой прогон**: `node loop/e2e/gate-M15a.live.ts` на бандле `artifacts/gate-M15a/source-bundle/` — интейк → цикл → SIGKILL посреди цикла → рестарт → возобновление → `COMPLETED` через чистый контейнер; артефакты `artifacts/gate-M15a/` целиком. Красные условия раунда 1 в силе; плюс дисциплина учёток: значение токена не попадает ни в один артефакт. Для наблюдаемости считай ходы и токены из stream-json, если CLI их отдаёт, — заказчику важно видеть цену цикла в его тарифных единицах (ходы/минуты), а не в долларах.
5. **Если подписочный путь в контейнере невозможен** — честная остановка с точным описанием (что читали, что запускали, что ответило), решение возвращается заказчику. Никаких обходов: чужие учётки хоста в контейнер не попадают, парк аккаунтов не строим (А-23).
6. **Рапорт** — секция «Раунд 2» сверху в REPORT-M15A.md, две строки в чат. Нового объёма нет: 159–167 и 170/171 не начинать.

## Не трогать

Прежний перечень платформы и пакета в силе целиком. Улики `.local/`: `db-m15a-stalled-040556` и текущий `db` хранить. Теги — после моей проверки артефактов и приёмки заказчика.
