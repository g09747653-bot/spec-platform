# START_HERE — точка входа исполнителя

> Пишет Архитектор. Фраза заказчика «Прочитай .specs/handoff/START_HERE.md и исполни» = вердикт ниже утверждён.

**Раунд:** M6 · раунд 1 · **Дата:** 2026-08-13 · **Режим сессии: Opus high** · **Сессия: новая**

## Гейт M5: ПРИНЯТ

9/9 чекбоксов, 1174 unit, 22 E2E×3. D-64…D-72 приняты без замечаний. Отдельно зачтено: CVE-2023-30533 в npm-`xlsx` с переходом на дистрибутив SheetJS (D-67 — именно наш путь кода, недоверенный вход), обнуление внешних ключей в E2E-конфиге, эксперимент слепоты гейтов (D-72), живой мини-смоук research. Урок стекированных PR записан верно.

## Хвост M5 — первым действием, с эмпирическим предусловием

1. PR-ужесточение: `BLOB_READ_WRITE_TOKEN` и `WEB_SEARCH_API_KEY` → `required(...)` (однострочный дифф из рапорта M5) + правка тестов env. **Предусловие проверяется деплоем:** если preview-деплой этого PR падает на этих переменных — их нет в Vercel; держи мерж, рапортуй одной строкой, дальше по M6 работай (это не блокер задач 72–80, кроме финального прод-смоука).
2. После зелёного мержа ужесточения: `git tag m5-accepted <зелёный main> && git push origin m5-accepted`.

## Задание: Milestone 6 — Export, Sessions & Completion (задачи 72–80) — MVP cut line

Это последний milestone parity-продукта. После него — полный путь prompt → interview → 4 файла → ZIP, целиком.

### Акценты Архитектора

- **72 — резолюция режима экспорта:** default-режим всегда резолвит последнюю pre-enrichment ревизию (задел под A6), `QualityPort = null` форсит default без staleness-запросов; `export_records` на каждый экспорт.
- **73 — манифест пропусков только в интерфейсе**, никогда внутрь архива (FR-015 AC-8); неполный бандл скачивается, а не отклоняется.
- **75 — session resume, повышенная тщательность** (карта режимов): восстановление каждого из четырёх типов pending-решения (вопросный раунд, approve спеки, diff, ревью) — ровно в ту же карточку; перезагрузка/выход/обрыв не меняют персистентного состояния. На этом стоит главный гейт проекта.
- **76 — каскадное удаление** включает Blob-объекты (IR-005-AC-3); подтверждение называет необратимость (DR-7).
- **77 — дубликат без pending-предложения** (по плану), гейты дубликата валидны с первого захода.
- **78 — sealed state:** из `complete` любой переход, кроме `complete → quality.collect`, отклоняется `SESSION_SEALED`; рефайнмент при этом жив (FR-020 AC-4).
- **79/80 — детерминированный харнесс и critical journey:** прогон полного пути без ручного вмешательства, архив содержит ровно четыре файла с точными именами. Помни: харнесс переиспользуется скелетным тестом, а не форкает его.

### Подготовка гейта (часть задания)

Гейт M6 — главный гейт проекта: заказчик пройдёт полный путь руками на **реальном провайдере** (это же покроет живые промпты M4 — ревью-борд, рефайнмент, чат-решения). В финальной секции рапорта дай заказчику готовую инструкцию запуска: локальный вариант (тест-БД + реальный Google-ключ из `.env`) и, если прод-деплой зелёный, прод-вариант. Инструкция — на уровне «две команды и адрес», как в M1.

### Рамки

PR-порядок прежний (D-19, правило в настройках постоянное). Объём — строго задачи 72–80 плюс хвост M5. Рапорт: `.specs/handoff/REPORT-M6.md`, в чат — две строки. **M7 не начинай.**
