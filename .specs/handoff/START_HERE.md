# START_HERE — точка входа исполнителя

> Пишет Архитектор. Фраза заказчика «Прочитай .specs/handoff/START_HERE.md и исполни» = вердикт ниже утверждён.

**Сессия:** M12п · раунд 2 — раскладка CI + живая прогулка (финал задачи 140) · **Дата:** 2026-08-18 · **Режим сессии: Opus HIGH** · Раунд короткий

## Вердикт по раунду 1: принят

Диагноз «поверхность была документом, а не приложением» как общая причина обеих рекламаций — точный, и замеры (46 px поля ввода на любом мониторе; верх сайдбара на 420 px выше вьюпорта) делают его неоспоримым. Аудит братьев, нашедший третий и четвёртый случаи того же класса; D-198/D-199 с WebKit, которые доехали бы до заказчика на Safari; починка робота, а не продукта, там, где печатал робот; и честное «до живой прогулки не дошёл» — всё принято. D-191…D-201 приняты. Таблица «взято как планка / сознательно иначе» — ровно тот формат, который нужен заказчику для финальной приёмки глазами; навигатор сообщений — в Backlog, согласен.

## Вердикт по §8 (А-15): раскладка утверждена

1. **Разложи e2e на три параллельные джобы** — «E2E (chromium)», «E2E (firefox)», «E2E (webkit)». Все три — обязательные чеки branch protection: строгость не меняется, меняется упаковка.
2. **Обнови список обязательных чеков через `gh api`** (аккаунт-владелец имеет право; это санкционированное изменение конфигурации, не обход — правило «no --admin, защиту не снимать» в силе). Если прав не хватит — остановись и выпиши заказчику точные шаги для UI одной секцией рапорта.
3. После раскладки верни таймаут к разумному (30 мин на джобу вместо 90 на монолит), перепрогони и смержи PR #55.

## Затем — финал задачи 140: живая прогулка

По профилю гейта: свежий ключ спереди (одна проверка живости разрешена — А-13), младшая локальная модель по предполёту; короткая прогулка по финальной оболочке, обе темы, деструктивные приёмы из твоего набора на живом стриме (вьюер над генерацией, тема посреди стрима, перезагрузки). Ноль обрезок, ноль структурных отказов, чистая консоль. Артефакты `artifacts/gate-M12/` (RESULT, TRANSCRIPT, скрины обеих тем — набор для приёмки заказчика глазами: 12 кадров из shots + живые состояния).

Зелёная прогулка → рапорт «Раунд 2» сверху в REPORT-M12.md, две строки в чат. **Теги не ставить:** финальную приёмку M12п выносит заказчик глазами по твоему набору скриншотов и живому продукту; я проверяю артефакты. Программу А не начинать.

## Не трогать

Продуктовые контракты (перечень раунда 1 в силе); визуальный слой раунда 1 не перерабатывать по ходу — прогулка фиксирует то, что есть, правки только на найденные дефекты с регрессиями.
