# START_HERE — точка входа исполнителя

> Пишет Архитектор. Фраза заказчика «Прочитай .specs/handoff/START_HERE.md и исполни» = вердикт ниже утверждён.

**Сессия:** M9п · раунд 2 (задачи 118, 120, 121, 122, 123) · **Дата:** 2026-08-15 · **Режим сессии: Opus high**

## Вердикт по раунду 1: принят. Все три вопроса §6 решены

Раунд сильный: парити-якорь, снятый **до** рефакторинга (снапшот с нового кода ничего бы не доказывал — именно так), каноническая семёрка стадий как алфавит машины (D-119), заголовки, парсящиеся из вендоренных шаблонов, а не выписанные руками (D-121), и разбор CI-таймаута до причины с двойным лечением (D-129). Остановка 118 на вопросе контракта — правильное применение уровней: вопрос был виден до работы, и ты не потратил ни одного подхода на неверный фундамент.

**Ответы по §6 (всё внесено в solution.md, амендмент А-6):**

1. **§6.1 — принято как есть.** `methodologies` внесён в таблицу границ solution.md с теми рёбрами, что уже стоят в `eslint.boundaries.js`. Документ приведён к зоне; ничего не меняй.
2. **§6.2 — снять UNIQUE: `PROJECTS ||--o{ SESSIONS`.** Твоё обоснование принято целиком. ERD исправлен, следствия зафиксированы в solution.md прозой: страница проекта становится списком чатов (120), поверхность сессии адресуется id сессии, **ссылка на проект с единственной сессией ведёт в её чат** (все существующие проекты открываются как раньше), экспорт остаётся проектным, дублирование (задача 77) копирует все сессии проекта — поправь его тесты. Миграция одна: `DROP CONSTRAINT` + колонки задачи 120 (`archived`, отображаемое имя чата) — как ты и предложил.
3. **§6.3 — прочтение подтверждено.** Две двери на ревью Requirements, выбор нажатием, без персистентного флага. D-120/D-125 верны, не переделывай.

## Объём: закрыть M9п (118 → 120 → 121 → 122 → 123)

Задача 119 сдана в раунде 1. Порядок именно такой: 118 первым (фундамент под 120 готов вердиктом), 121/122 — Parallel-safe, если где-то упрёшься.

Особые указания к раунду 2:

1. **118 (Edit):** атомарность мультифайлового применения — транзакция, тест с индуцированным сбоем посредине; reject = байт-в-байт (M4). Ревизии, написанные Edit-сессией, несут её id как источник — история файла обязана называть, какой чат её произвёл. Reference-шаг предлагает только файлы с одобренными ревизиями.
2. **120:** архивация обратима и ничего не удаляет; «Last message Nd ago» — из персистентных строк, не из клиентских часов; MCP-карточка без единого сетевого вызова.
3. **121:** слэш-команды и @-ссылки — те же эндпоинты, что кнопки (тест тождества состояния БД по образцу M4); модели без ключей скрыты; Auto = существующая цепочка; выбор модели персистится на сессии и доказуемо меняет вызов адаптера (шпион на маршруте).
4. **122:** Raw байт-в-байт через эндпоинт задачи 74; Diff Rev 1 честно говорит «предшественника нет»; состояние вьюера в URL.
5. **123 (гейт):** живой набор из START_HERE раунда 1 в силе + твой же урок D-129: полное путешествие в сьюте стоит трёх — не добавляй лишних живых маршрутов сверх заданного набора. Снимки с `caret: 'initial'`. Счётчики линтеров на requirements/tasks досках — в RESULT. Артефакты `artifacts/gate-M9/`.
6. **Не трогать:** Р-1/Р-2/Р-3, D-99…D-101, парити-якорь `transition-table.parity.json`, побайтовое тождество дефолтного экспорта.

## Стоп-правила

Прежние: красный CI = стоп; 2 подхода → BLOCKED; идеи «заодно» → Backlog. **M10п не начинай.** Рапорт — новой секцией сверху в `REPORT-M9.md`, в чат заказчику — две строки по протоколу.
