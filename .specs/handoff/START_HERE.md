# START_HERE — точка входа исполнителя

> Пишет Архитектор. Фраза заказчика «Прочитай .specs/handoff/START_HERE.md и исполни» = вердикт ниже утверждён.

**Сессия:** Fix-раунд приёмки Программы А (до финализации) · **Дата:** 2026-08-22 · **Режим сессии: MAX**

## Что случилось

Финальная приёмка заказчика прервана: его задумка («сайт — графическая копия nvidia») прошла спецификацию целиком (79 шагов, 58 мин), но машинный бандл вышел подозрительно тонким (13 КБ на четыре файла против обычных 27–60), и `start-loop` отверг его named-400: «/tasks: ноль задач — схеме соответствует, но исполнять нечего». Отказ ПРАВИЛЬНЫЙ — мусор в конвейер не пошёл; дефект выше по течению. Твоё отклонение при подъёме стека (build+start вместо loop:dev) одобрено и становится нормой (А-32 п.1).

## Порядок раунда

1. **README**: §«Контур доставки» и §«Задумка из Telegram» — на build+start (или скрипт `loop:serve`); починка dev-режима — Backlog-строкой.
2. **Диагноз сужен фактом заказчика (2026-08-22): источники полновесны, выжимки пусты.** Его листинг бандла: constitution.md 16 КБ, architecture.md 22 КБ — а requirements.json и tasks.json по ~1 КБ (схемо-валидные скелеты); «13 КБ» алерта было размером АРХИВА, гипотеза (б) снята. Диагноз — гипотеза (а), охватом на ОБЕ выжимки: маппинг не распознал форму записи этой сессии ни в tasks-документе, ни в requirements-документе (класс D-247/169, новая форма). Сырьё: проект ecec2f2f, сессия 00c30f45, бандл `loop/.data/workspace/tg-20260822-203415`. Починка: форма — в терпимость обоих маппингов, живые документы этой сессии — в goldens, регрессии на оба; заодно установи, ЧЕМ форма этой сессии отличалась (мост/sonnet против прежних прогонов — почему канон 169 не удержался) и, если лечится строкой инструкции, добавь её.
3. **Ранний отказ вместо позднего — по обеим выжимкам**: отказ после 58 минут — поздний, а пустые requirements вообще проскочили молча (интейк отверг только задачи). Смок-проверка сразу после машинного экспорта, ДО алерта «Бандл получен»: ноль строк ЛЮБОЙ из двух выжимок при непустом источнике → named-алерт немедленно; интейк контура получает то же красное условие на requirements. Реализация — твоим решением по месту, в объёме раунда.
4. **Повторный прогон**: перезабор машинного бандла той же сессии → `start-loop` → до продукта (алерты пойдут заказчику в TG). Если по диагнозу (б) спека негодна — честный вердикт в рапорте, и заказчик пришлёт новую задумку вместо повторного прогона этой.
5. **Рапорт** — секция сверху в REPORT-M17a.md («Fix-раунд приёмки»), две строки в чат.

## Не трогать

Прежний перечень целиком. Задачи 177/178 и финализация — НЕ в этом раунде (сдвинуты за успешную приёмку). Хук transcribe — шов. Учётки не печатать.
