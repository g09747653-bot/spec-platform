# START_HERE — точка входа исполнителя

> Пишет Архитектор. Фраза заказчика «Прочитай .specs/handoff/START_HERE.md и исполни» = вердикт ниже утверждён.

**Сессия:** M8п · Review-цикл (задачи 111–115) · **Дата:** 2026-08-15 · **Режим сессии: Opus MAX**

## Вердикт по M7п: ПРИНЯТ

Проверено по коду, артефактам и PR #37/#38: `build-feed.ts` действительно чистая проекция с выведенными чипами и причинным тай-брейком (D-102 — образцовое решение), гейт GREEN на 57 состояниях с сохранением всех M6-проверок дословно, обе находки гейта (D-109 и дефекты скрипта) вскрыты честно. D-103 (дверь предлагается и при закрытом гейте, с причиной) принимаю — P1 применён верно.

**А-4 внесён Архитектором:** solution.md §API «Chat decision routing» приведён в соответствие с кодом (NDJSON-доставка, финальное событие несёт прежний контракт). Расхождение из твоего п. 6 закрыто, код не трогать.

## Объём сессии: только M8п (111 → 115 по порядку)

Суть milestone: ревьюер, который делает продукт «умным». Эталон поведения — `.specs/research/myspec-parity-reference.md` §1.3 **и стенограмма завершённой сессии оригинала в §1** (структура находок, сводка, confidence, Suggestion, абзац «что я сложил и какие решения принял сам», повторная верификация именно выбранных пунктов). Калибруй промпт ревьюера против неё.

Особые указания Архитектора:

1. **На входе — таймбоксированный разбор гидрационного предупреждения** из Backlog (5 из 57 состояний M7п-гейта): один полноценный подход. Воспроизвёл — почини корнем; нет — запиши наблюдение с гипотезой в decisions.md и иди дальше. Вслепую не чинить.
2. **111 (review.v2):** схема с двумя группами и confidence 1..10; разбор — Р-1 (внешний сбалансированный JSON + ровно один повтор). Доски персистятся как история: **ни одна ревизия доски и документа никогда не перезаписывается** — только добавление.
3. **112 (карточка):** Must Fix отмечены по умолчанию, Recommendations сняты; состояние чекбоксов едет с решением и фиксируется в истории после решения. Три кнопки — семантика Эталона §1.3 дословно: Accept = дальше как есть, Ignore = отбросить, Request = 113.
4. **113 (цикл):** сообщение «что сложил» перед Rev N+1 — обязательная часть контракта; повторное ревью получает выбранные пункты и **не пересуживает невыбранные** (фикстура); цикл ограничен конфигурируемым максимумом с честной копией по паттерну gate-copy. Расширение transition-table — матричный тест обязан снова покрывать 100% рёбер (правило гейта M2).
5. **114 (линтеры У-3):** ни одного вызова модели; находки вливаются в доску машинными пунктами (source `linter`, confidence 10) в Must Fix. Три обязательные фикстуры: битая кросс-ссылка, перенумерованный идентификатор, не-EARS строка требования.
6. **Не трогать:** Р-2/Р-3 контракты, D-99…D-101, проекцию ленты (новые блоки доски — через расширение проекции, без write-path), решённые доски M4-механики.
7. Гейт M8п (115) сам по А-2.1: живой цикл needs-revision → request changes с подмножеством пунктов → Rev N+1 → повторное ревью → pass → accept; текстовая фраза-решение проверяется живьём наравне с кнопкой. Артефакты в `artifacts/gate-M8/`, лёгкий trace. Цепочка `google,ollama` легитимна; помни, что дневная квота Google — 20 запросов, ревью-цикл прожорлив — прогон может целиком уйти в ollama, это нормально.

## Стоп-правила

Прежние: одна сессия = один milestone; красный CI = стоп; 2 подхода → BLOCKED; идеи «заодно» → Backlog. **M9п не начинай** (его декомпозицию Архитектор уточнит на этом гейте). Рапорт — новой секцией сверху в `.specs/handoff/REPORT-M8.md` (новый файл), в чат заказчику — две строки по протоколу.
