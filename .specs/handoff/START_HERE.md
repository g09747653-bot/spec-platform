# START_HERE — точка входа исполнителя

> Пишет Архитектор. Фраза заказчика «Прочитай .specs/handoff/START_HERE.md и исполни» = вердикт ниже утверждён, включая приёмку заказчика глазами.

**Сессия:** M15а — ядро оркестратора и одиночный цикл handoff (Программа А) · **Дата:** 2026-08-19 · **Режим сессии: MAX**

## Вердикт по M14а: 148–151 ПРИНЯТЫ (А-21)

Milestone принят целиком. Поимённо зачтено: одна точка подмены на шве `currentOwnerScope()` без единой ветки ниже; форграунд-супервизор вместо detached — замеренное решение, не вкусовое; находка гейта (AJV-валидный tasks.json с нулём задач из непустого источника) закрыта образцово — новая форма в маппинге, живой golden, красное условие «ноль строк при непустом источнике», дозабор с sha256-доказательством нетронутых ревизий; репетиция бухгалтерии на стабе до живого прогона — правильный вывод из урока A0. D-241…D-248 приняты.

Ответы на §7: **(1)** строка `GET /api/projects/:id/export/machine` внесена мною в solution.md §API. **(2)** Закрепляем на стороне контракта, не отказываясь от терпимости, — это задача 169 этой сессии: инструкция генерации tasks-документа получает каноническую форму задач и явную нотацию зависимостей; маппинг держит все три формы как терпимость, фазовый порядок — фолбэк. Я проверил источник твоего `dependsOn: []` — фазовый документ действительно не называет зависимостей; маппинг честен, но планировщик контура на таком бандле слеп, лечится контрактом. **(3)** Полная версия схемы фикстурой — верно; расхождение копий внутри бандла A0 записано как дефект самого бандла.

## Порядок M15а

**Нулевой шаг — preflight Docker**: `docker version` через named pipe. Заказчику сказано поставить Docker Desktop до сессии; если Docker недоступен — честная остановка с рапортом (что именно не отвечает), не BLOCKED и не самодеятельная установка.

Дальше — задачи 152–158 + 168 + 169; читай их полные тела в tasks.md (блок M15а детализирован при приёмке). Что я требую сверх написанного:

1. **Порядок сборки:** 168 и 169 — первыми (обе Small, обе на стороне платформы, обе разблокируют остальное: 168 снимает мину под рестарт-сценарии гейта, 169 даёт контуру бандлы с настоящими зависимостями). Затем 152 → 153/154 (154 параллелен) → 155 → 156 → 157 → 158.
2. **Диск — источник правды, база — индекс.** Любое состояние, которое нельзя восстановить из `handoff/`-дерева, — дефект проекта, а не деталь: этим контур отличается от «скрипта с базой».
3. **Вехи режет код** (топология по `dependsOn`, фазовый фолбэк), модель наполняет тексты заданий через провайдерный шов из `.env` — захардкоженного anthropic не существует. Это зеркало D-229, и оно здесь закон.
4. **Чистый контейнер решает гейт.** Отчёт исполнителя — информация для ленты; `COMPLETED` ставит только независимый перепрогон. Тест «исполнитель говорит SUCCESS, чистый контейнер красный» обязателен.
5. **Секреты:** `.env` целиком в контейнер не попадает никогда; проверка изнутри контейнера — тестом. Точечная передача — единственный путь.
6. **Аутентификация Claude Code в контейнере** может потребовать операционного шага заказчика (вход/токен) — это именованная остановка с точным описанием нужного, не дефект и не BLOCKED-тупик.
7. **Гейт 158** — на настоящем бандле машинного экспорта платформы (путь задачи 150), не на рукописной фикстуре: контракт должен встретиться живьём. SIGKILL оркестратора посреди цикла и возобновление — обязательная часть прогулки.

## Не трогать

Прежний перечень платформы в силе (state-machine, А-8/А-10, heartbeat, Р-контракты, бюджеты, парити-якорь, побайтовый ZIP-экспорт, immutability, реестр строк платформы). Скелеты M16а–M17а (159–167) не начинать. Backlog сверх 168 не реализовывать. Черновой и локальный профили БД не трогать. Рапорт — новая секция сверху в REPORT-M15A.md (новый файл), в чат — две строки. Теги — после моей проверки и приёмки заказчика.
