# START_HERE — точка входа исполнителя

> Пишет Архитектор. Фраза заказчика «Прочитай .specs/handoff/START_HERE.md и исполни» = вердикт ниже утверждён.

**Сессия:** M16а · раунд 2 — пределы времени по данным и гейт 163 двумя прогонами · **Дата:** 2026-08-21 · **Режим сессии: MAX**

## Вердикт по раунду 1: 170–173 и 159–162 ПРИНЯТЫ (А-26); остановка гейта — правильная

Раунд принят. Поимённо зачтено: изоляция 170, опровергшая гипотезу твоего же рапорта прочтением улики напрямую, — трихотомия `step_outcome` точна, и три GREEN-прогулки подряд на убивавшей позиции закрывают вопрос; поправка к собственному рапорту про loop:e2e в CI (и правильно отвергнутый дубль шага); порядок заморозки с зеркальным снятием; `livePipeline` вместо догадки; и три поправки к замерам — различение «пика нет» от «замер не состоялся» дороже безошибочного отчёта. D-277…D-295 приняты. Числа measurements.json я сверил по сырому файлу — сходятся. Второй чужой объём (reload-помощник) легализован тем же доводом, что и первый.

Ответы на §7 (полные формулировки в А-26): **(1) потолок итерации — 900 с умолчанием** (3× p90; задний упор, не рабочий предел — довод D-234) плюс необязательное `iterationTimeoutSec` в схеме задачи; «нет вывода N секунд» отвергнут — молчание не смерть (урок Р-4/А-9). **(2)** Да — свой предел на прогон тестов приёмки (300 с умолчанием, именованная причина); потолок приёмки остаётся упором. Оба — задача 174. **(3)** Модельный путь интейка — перепроверить дёшево на свежей квоте. **(4)** Для замера тарифа — второй бандл под ширину. **(5)** MaxListeners — без задачи, перепроверить на широком прогоне. **(6)** VRAM собирает этот раунд починенным монитором.

## Порядок раунда 2

1. **Задача 174** (полное тело в tasks.md): умолчание 900 с + per-task `iterationTimeoutSec` (аддитивно, старые файлы валидны) + собственный предел прогона тестов приёмки с именованной причиной. Регрессии на всё.
2. **Перепроверка модельного пути интейка** (вердикт §7.3): на свежей дневной квоте — интейк одного бандла с заданиями от МОДЕЛИ (не фолбэком), сверка что тексты модельные и валидные; полный конвейер не нужен. Квоту гейта до живых прогулок не жечь (правило прежнее).
3. **Широкий бандл**: автономный прогон платформы с seed под ширину — веха на 8–10 НЕПЕРЕСЕКАЮЩИХСЯ задач (например «набор из десяти независимых консольных утилит, каждая в своём каталоге, с общим README»); машинный экспорт → source-bundle-wide.
4. **Гейт 163 — два живых прогона**:
   - **Прогон А** — текущий бандл до **19/19** с новыми пределами; это закрывает «план до конца».
   - **Прогон Б** — широкий бандл: замер тарифа под настоящей параллелью (цель — увидеть пик ≥8 исполнителей или упор в лимит подписки; и то и другое — результат), все `rate_limit_event` в артефакты, VRAM-пик починенным монитором, наблюдение MaxListeners при ≥8 контейнерах. Инсценированный красный и SIGKILL повторять не обязательно — они доказаны прогоном раунда 1; красные условия (утечка, database is locked, каскад на троттлинге) в силе.
5. **Рапорт** — секция «Раунд 2» сверху в REPORT-M16A.md, две строки в чат. Чекбокс 163 — после GREEN обоих прогонов; 174 — после регрессий.

## Не трогать

Прежний перечень платформы и пакета в силе. Скелет M17а (164–167) не начинать. Улику `.local/db-m15a-stalled-040556` можно вернуть заказчику на удаление — задача 170 закрыта; текущий `db` хранить. Значения учёток не печатать нигде. Теги — после моей проверки артефактов и приёмки заказчика (обоих раундов разом).
