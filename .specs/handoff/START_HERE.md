# START_HERE — точка входа исполнителя

> Пишет Архитектор. Фраза заказчика «Прочитай .specs/handoff/START_HERE.md и исполни» = вердикт ниже утверждён.

**Сессия:** M10п · Визуальный слой и финал этапа 1 (задачи 124–127, 131, 128, 129 — в этом порядке) · **Дата:** 2026-08-16 · **Режим сессии: Opus high**; на задаче 128 — один ultracode red-team прогон по карте режимов §3-bis

## Вердикт по M9п: ПРИНЯТ. Milestone закрыт

Гейт зелёный, и раунд 5 — образцовый по методу: сам забраковал зелёную прогулку 1 как доказательство Р-4 (все вызовы ушли в Google — молчания, ради которого писался heartbeat, не было), поставил прогулку 2 намеренно ради этой тишины, поймал в ней собственную ложноположительную проверку (D-160 — второй случай паттерна D-154, честно назван) и предел локальной модели (D-161), записал раунд чистой прогулкой 3. Heartbeat доказан тремя молчаниями по 4,3–4,5 минуты. Косвенное доказательство ремонта рекомендаций принято достаточным; строка в лог — в задаче 131 (см. ниже). D-159…D-161 приняты.

**Первым действием сессии:** тег `m9p-accepted` на текущий `main`, запушить.

## Вердикты Архитектора, внесённые в бандл

1. **А-10 (по твоему §7.2, D-161): вариант (а).** Локальный адаптер вызывает структурированные артефакты с грамматически-ограниченной генерацией (Ollama `format` + JSON-схема). (б) отвергнут — ломает атомарность 118; (в) отвергнут — противоречит А-7. Не тянет даже под ограничением — именованное ограничение локального режима, не молчаливый уход в облако. Это задача 131.
2. **По твоему §7.1:** косвенного доказательства для приёмки хватило; строка в лог от `atMostOneRecommended` — обязательна, вошла в AC задачи 131.
3. **M10п декомпозирован** — читай блок «Milestone 10п» в tasks.md целиком: 124 (токены и тема со **своим** оттенком — не их палитра), 125 (лоадер/connection-lost/тосты), 126 (completion-панель + Generate AI Prompt + честные copy-and-open кнопки платформ), 127 (дифф-полировка Edit + «go back» как append-ревизия), 131 (А-10), 128 (парити-чек-лист + ultracode red-team), 129 (финальный гейт + печать этапа 1).

## Особые указания

1. **«Свой оттенок» — буквально:** структура токенов и типографика по образцу эталона, палитра и бренд — наши собственные. Их фирменные цвета (#5939e2/#02dede/#a249f9) не копировать.
2. **126 — никакой имитации интеграций:** Generate AI Prompt ссылается на реальные одобренные ревизии; кнопки платформ — копирование промпта + открытие сайта, и UI говорит это прямо.
3. **127 — revert только append'ом:** immutability-триггеры ревизий (задача 16) — не трогать; «go back» = новая ревизия с содержимым предыдущей.
4. **128 — чек-лист строится из эталона** (`.specs/research/myspec-parity-reference.md` Часть 1, §1.1–1.5), вердикт каждому пункту: парити / осознанное отличие своего оттенка / разрыв. Разрывы — чинить в 129 или нести мне на приёмку как принятое отличие; сам не решай, что «и так сойдёт».
5. **Гейт 129 по новому профилю** (decisions.md, 2026-08-16): свежий ключ спереди (он уже в `.env.local`, за ~50 вызовов раунда 5 — ноль `RESOURCE_EXHAUSTED`); предполёт меряет `qwen3:8b` (структура + JSON-раунд + ограниченный Edit) — младшая проходит, берёшь её, нет — остаёшься на `qwen3:14b`; квоту в день гейта до прогулки не тратить; снимки с `caret: 'initial'`; лёгкий trace.
6. **Не трогать:** А-8-упаковку, heartbeat/Р-4, Р-1/Р-2/Р-3, D-99…D-101, парити-якорь, побайтовый экспорт, immutability ревизий.
7. Milestone, вероятно, на две сессии; естественный шов — после задачи 127 (перед 131). Правила прежние: рапорт «Раунд N» сверху в `.specs/handoff/REPORT-M10.md` (новый файл), в чат — две строки.

## После гейта 129

Зелёный гейт + мой вердикт по чек-листу = **этап 1 закрыт** (теги в задаче 129). Дальше — Программа А по А-7: её ТЗ генерируем нашим же продуктом; план подготовит Архитектор на приёмке M10п. **Ничего из Программы А не начинать.**
