# START_HERE — точка входа исполнителя

> Пишет Архитектор. Фраза заказчика «Прочитай .specs/handoff/START_HERE.md и исполни» = вердикт ниже утверждён.

**Сессия:** А-37 — бенчмарк копии С НУЛЯ: светлая англоязычная вёрстка конвейером в чистой директории · **Дата:** 2026-08-24 · **Режим сессии: MAX**

## Вердикты по раунду А-36 (А-37)

Раунд зачтён как честный и неполный. Зачтено поимённо: автономный проход тремя кнопочными ходами; сборка целиком одной задачей; честная остановка на полировке с named-противоречием собственного брифа — страж работал; снятие тобой же некорректной подачи «49,38 против 44,16» ДО публикации — дисциплина замера; суд трёх осей, не подыгравший своему конвейеру. Признано и другое: сборку с нуля раунд не измерил (конвейер работал, где артефакт уже лежал), попиксельно — ничья, и в одном плане жили несовместимые цели (NEURA против светлого эталона) плюс самописные ворота «≤1%», недостижимые по построению.

## Решение по BLOCKED WA04: вариант 2 (мой уровень, А-37 п.2)

Бенчмарк судится на артефакте, совпадающем с эталонами ПО ПОСТРОЕНИЮ всем, кроме знака: **светлая, англоязычная, состав/порядок секций и фактический контент оригинала, нейтральный знак**. Палитра и язык из метрики больше НЕ исключаются (совпадут по построению); исключение — только знак/бренд-зоны (А-36.1). Вариант 1 отклонён (подменяет вопрос бенчмарка), вариант 3 отклонён (выхолащивает метрику). **NEURA не отменяется как работа** — дизайн-решения А-35 остаются принятыми, артефакт сохрани уликой (копия в artifacts/ или отдельный каталог); бенчмарк-артефакт строится новый.

## Порядок сессии

0. **Пуш и алерт прошлого раунда**: заказчик разрешает `git push` (или пушит сам) — ветка `feat/a36-whole-artifact-plan` уходит PR-ом, мерж по зелёному CI; финальный алерт А-36 отправь ботом как есть — честная фиксация.
1. **Правка ветки архитектора заданий**: числовой порог НЕ бывает воротами polish/замер-задач — приёмка полировки = «замер прогнан и зафиксирован + сходимость (расхождение не растёт между итерациями)»; порог — публикуемая метрика алерта. Регрессия на слепке WA04 (план с воротами ≤1% больше не генерится).
2. **Бенчмарк-прогон варианта 2 — конвейером, с нуля, без рук:**
   - **Чистая рабочая директория** (новый workspace) — это закрывает «с нуля не измерено». Материал скопировать как сырьё: 141 изображение, шрифты, 87 эталонов с манифестом (добыча законна). Подготовка среды — операторская, назови её.
   - SEED — задумка-копия: светлая тема, en-US, состав/порядок секций и фактический контент оригинала, нейтральный знак.
   - Путь целиком: `start-loop` → классификатор (`coherent-artifact`) → цельный план (сборка целиком одной задачей + полировка по п.1 + замер) → исполнитель в контейнере строит С НУЛЯ → суд трёх осей (обязателен зелёный — планка продукта) → попиксельный замер против 87 эталонов, публикуется как есть (знак-зоны отдельной строкой).
   - Дисциплина: сужение брифов запрещено; ходы только кнопочные, поимённо; per-task таймауты ДО старта — законная настройка среды.
3. **Итог заказчику**: алерт со сверкой + один вход; его глаза — приёмка бенчмарка.
4. **Рапорт** — секция «Бенчмарк А-37» сверху в REPORT-M17A.md, две строки в чат.

## Не трогать

Прежний перечень целиком. NEURA сохранить. Задачи 177–180 и финализация — после вердикта заказчика по бенчмарку. Учётки не печатать. Дозабор материала с живого сайта запрещён, как и был.
