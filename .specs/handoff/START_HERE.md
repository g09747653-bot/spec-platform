# START_HERE — точка входа исполнителя

> Пишет Архитектор. Фраза заказчика «Прочитай .specs/handoff/START_HERE.md и исполни» = вердикт ниже утверждён, включая приёмку заказчика глазами.

**Сессия:** M17а — Telegram и голос: финальный milestone задумки · **Дата:** 2026-08-21 · **Режим сессии: MAX**

## Вердикт по раунду 2: M16а ПРИНЯТ ЦЕЛИКОМ (А-27)

Оба прогона зачтены; числа run-b я сверил по сырому файлу — сходятся до строки (45 итераций, пик 10 из 10, ровно десять исполнителей 139 секунд, 45 тарифных строк `allowed`, VRAM с пятикратным запасом). Замер тарифа, заказанный А-24 п.2, состоялся: подписка выдержала полную ширину без единого троттла. Поимённо зачтено: оба предела задачи 174 сработали вживую в первый же час — и незавершающийся тест, и тяжёлая задача, которая при старом потолке была бы убита; операторское спасение через пометку предела на диске с отказом поднимать дважды (D-303); спасение через бриф, а не через код проекта (D-306); мост подписки без единой изменённой строки платформы с тремя замеренными уроками CLI (D-304). D-296…D-306 приняты.

Ответы на §7 (полные формулировки в А-27): **(1)** мост становится штатным адаптером — задача 175; строку в solution.md §adapters/llm внесу при её приёмке. **(2)** Гейты платформы переходят на цепочку «адаптер спереди, google запасным»; новый ключ заказчику не нужен. **(3)** MaxListeners — Backlog-строкой, задачи нет (добавь строку). **(4)** Улика — заказчику на удаление, подтверждено. **(5)** Мелочь `GATE_RESTART=0` — первой в этой сессии. Находки §6 — задача 176.

## Директивы заказчика после вердикта (А-28) — прочитай до начала

Пять уточнений внесены в decisions.md (А-28) и в тела задач 164/165: (1) chat id лежит в `chat_id.txt` в корне — перенеси в `loop/.env` как `TELEGRAM_OWNER_CHAT_ID` и удали файл; я его прочитал — там только идентификатор, БЕЗ токена бота: токен придёт от заказчика файлом `env.loop.txt` (строка `TELEGRAM_BOT_TOKEN=…`) — нет файла к началу 164 → именованная остановка; (2) транскрибация — СНАЧАЛА зонд подписочного звена (аудио через адаптер 175/CLI, качество на русской речи), Whisper — запасная опция конфига; (3) текст обязателен наравне с голосом; (4) 164 — полноценный интерфейс: меню команд, `/start`-онбординг, `/status`, `/stop`, кнопки; (5) бот закрытый: чужие сообщения — молча, включая `/start`.

## Порядок M17а

1. **Теги первым действием**: `m16a-accepted` на смерженный main.
2. **Мелочь §7.5**: `GATE_RESTART=0` в `e2e/gate-M14a.live.ts` не пишет PROBLEM «рестарт не исполнен» — вердикт при явно выключенном рестарте не должен лгать ярлыком.
3. **175 → 176 → 164 → 165 → 166 → 167** — полные тела в tasks.md (блок M17а). Сверх написанного требую:
   - **Опциональность шлюза.** Без TG-токена и без Whisper контур работает ровно как вчера; каждый незаведённый конфиг отвечает именованно, не молчит.
   - **Суверенитет на входе.** Распознанный текст — решение владельца: кнопки «Запустить»/«Отмена» до каких-либо действий; чужие chat id игнорируются молча.
   - **Фасад — только существующие поверхности** платформы (А-20 п.3в). Платформа не меняется ни строкой; если чего-то не хватает — стоп и вопрос мне, а не новый эндпоинт.
   - **Секреты**: токены бота и подписки не печатаются нигде; красные условия на утечку — в каждом артефакте гейта.
   - **Гейт 167** — сквозной текстовый путь без рук, от сообщения боту до алерта успеха; фикстурная транскрибация тем же путём. Живой голос НЕ имитировать — голосовая задумка живьём есть финальная приёмка заказчика, её место после моей проверки артефактов.
4. **Рапорт** — секция сверху в REPORT-M17A.md (новый файл), две строки в чат.

## Не трогать

Прежний перечень платформы и пакета в силе целиком. Backlog не реализовывать (MaxListeners — строкой в Backlog, не задачей). Улику `.local/db-m15a-stalled-040556` можешь удалить по подтверждению А-27 (или оставить заказчику — его данные). Теги — по п.1; финальная приёмка — моя проверка артефактов, затем заказчик голосом.
