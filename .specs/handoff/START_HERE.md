# START_HERE — точка входа исполнителя

> Пишет Архитектор. Фраза заказчика «Прочитай .specs/handoff/START_HERE.md и исполни» = вердикт ниже утверждён.

**Сессия:** А-38 — «без нажатий» + стенд контура · **Дата:** 2026-08-24 · **Режим сессии: MAX**

## Сначала — отмена ложной посылки (А-39)

Заказчик усомнился и оказался прав, а я принял твой диагноз по рапорту, не открыв папку. Проверено лично: в `tg-20260822-235200/assets/images/content` **141 файл, 17 МБ** подлинной продуктовой графики оригинала (герой-баннеры 1920×1080 и 2560×1440, продуктовые кадры линейки, логотипы), и **135 из них артефактом использованы**. Строка рапорта А-36 «продуктовых изображений оригинала в добытом материале нет» и мои производные от неё (**named ceiling** в А-36.1/А-37/А-37.1) — **неверны и отменены**. Корректно так: артефакт ссылается на ~301 изображение, в материале 141 — материал покрывает **около половины**, а не «ничего». Потолок есть, он вдвое меньше и не «недостижим по построению». Смена эталона на Story не была необходимой; ошибка моя, не твоя — ты назвал диагноз открыто, проверить обязан был я. **Раунд Story не отменяется**: D-324/D-325/D-326, сборка с нуля и закрытый вопрос лживой галочки от эталона не зависят.

**Правило с этого момента для любой задумки-копии:** строй максимум возможного из того, что законно есть; **никогда не останавливайся ради разрешения на материал**; расхождения объявляй **заранее, ботом, ДО показа результата** — что не воспроизведено, почему и что поставлено взамен (`DEVIATIONS.md` + вершинная строка А-33; новое только то, что предупреждение идёт до продукта). Человек получает объяснение, а не вопрос.

## Приёмка раунда А-37.1

**Раунд принят как измерение. Главный вопрос А-37.2 закрыт положительно: лживой галочки нет.** Осмотрел лично, не по рапорту: board.json с уликами живости, behavior-report, visual-diff покадрово, PLAN_REVIEW, задачи и рапорты, чистоту директории (подтвердил независимо — вне `site/` ни одного файла оригинала, только три файла харнесса) и попарно сличил кадры глазами. **Мой глаз сошёлся с машинным вердиктом** — ради этого замер и затевался.

Зачтено поимённо: путь с нуля в чистой директории без единой правки руками; 6 из 6; самозамер без лести (25.02%, `passed:false`, три нарушения — и прогон доведён); суд, назвавший красную ось на собственном продукте; три скрытых дефекта, найденных и починенных по ходу; снятые наивные 75.19% в пользу честных 72.05%; лицензия исполнена.

**Находка Архитектора, меняющая чтение числа — и это дыра МОЕЙ спецификации.** В материале оказались только иконочные шрифты (15 файлов `fa-*`); текстового начертания эталона нет — оно живёт на Google Fonts и названо в HTML, который я справедливо запретил показывать. Копия набрана системными стеками (`Georgia … serif` в заголовках против гротеска эталона). Отсюда один каскад, а не три дефекта: чужие метрики → другие переносы → другие высоты секций → все три «нарушения поведения». Покадрово это видно: верх 14–85%, низ 1.0–4.7%. **72.05% — пол твоей способности, а не потолок.** Пересъёмку ради лучшего числа НЕ назначаю: погоня за метрикой запрещена, вопрос был другой и на него отвечено. Правило на будущее записано в А-38 п.3.

## Порядок сессии

0. **Алерт А-37.1 отправить как есть** — он честен и полон; мою находку по типографике в него не вписывать (алерт — самоотчёт контура, находка сделана постфактум Архитектором; каналы не смешивать). PR #80 — мерж по зелёному CI.
1. **ГЛАВНОЕ: конвейер переходит между вехами сам.** Твоя четвёртая находка становится работой сессии, а не хвостом. Сегодня «без рук» означает «без правок», но не «без нажатий» — это прямой разрыв с North Star и прямой ответ на вопрос заказчика «почему я в середине контура». После завершения работы разблокированная задача обязана подхватываться без внешнего хода; проход `driveProject` не заканчивается там, где очередь не пуста. Троттлинг, заморозка, красный CI и потолок итерации остаются законными остановками — «сам идёт» не значит «не умеет встать». Регрессия: проект из двух вех, вторая разблокируется завершением первой, ни одного внешнего хода — доска доходит до конца сама.
2. **СТЕНД КОНТУРА — вместе с п.1, потому что п.1 и есть его первый клиент.** Отладка управляющего потока сегодня стоит трёхчасового живого раунда; это и есть причина, по которой баги идут поштучно. Подменяется **ровно граница вызова контейнера**: заглушка-исполнитель возвращает настоящую форму рапорта за миллисекунды, всё остальное (план, вехи, раздача, статусы, перезаход, доска, алерт) работает по-настоящему. Объём жёсткий и не растёт: заглушка + пять инвариантов + короткая лестница щупов. **Это не фреймворк тестирования** — начнёт разрастаться, останавливайся и докладывай.
   - **Пять инвариантов**, каждый проверяется на переходе состояния: доска равна диску; проект не видит строк чужого проекта; статус «принято» невозможен без контейнера; перезаход даёт тот же план; счётчик доски равен числу файлов задач.
   - **Лестница щупов**: 1 задача → 6 → 20 → 50 → два проекта одновременно; каждый прогон машинно проверяем.
   - **Обязательная живая смок-задача** в каждом прогоне стенда — одна, настоящим исполнителем в контейнере. Без неё заглушка становится вторым домом лживой галочки: разойдётся с настоящим исполнителем — и стенд будет зелёным при сломанной работе. Это названный главный риск затеи, и смок — его единственное лечение.
   - **Честная граница пользы**: стенд ловит класс «труба и состояние» и НЕ ловит класс «столкновение с реальностью» (глаза вне контейнера, симлинки, потолки времени, шум консоли, нехватка материала). Живые раунды остаются; стенд лишь перестаёт быть единственным способом найти баг в трубе. В рапорте назови, какие из пяти инвариантов сработали на п.1 — если ни один, скажи это прямо, затея себя не окупила.
3. **Остаток D-325 — составной ключ.** Первичные ключи задач и вех: `(project_id, task_id)` и `(project_id, milestone_id)`. Регрессия: два проекта с совпадающими идентификаторами задач в одной базе не видят строк друг друга.
4. **Честная граница «без нажатий»** — после п.1 назови в рапорте поимённо, какие человеческие ходы ОСТАЛИСЬ в прогоне от задумки до продукта и почему каждый из них там. Это вход в следующую работу, а не украшение рапорта.
5. **Следующим прогоном — nvidia по живому ТЗ заказчика** (не в этой сессии; назови готовность в рапорте). Прогон идёт **по новой семантике слова «копия» (А-41б), и её надо прочесть до того, как планировать**: «копия» по умолчанию — узнаваемое воспроизведение состава, порядка, стилистики и поведения, **а не пиксельное тождество**; стопроцентная планка возникает только если человек её произнёс. Пиксельный процент — наше диагностическое число, не мера успеха задумки. Контур обязан рассудить о выполнимости ДО сборки, объявить это, сделать возможное и возместить недостающее той же стилистикой. Уточняющий вопрос человеку разрешён и нормален — но только о СУТИ и мере задумки, один раз, до сборки; о материале и разрешениях не спрашивают (А-39). Условия среды: прогон после п.1, то есть уже без нажатий.
6. **Рапорт** — секция «А-38» сверху в REPORT-M17A.md, две строки в чат.

## Читать перед планированием любой вехи

`.specs/hazards.md` — карта режимов отказа контура по одиннадцати стадиям (вылечено / правится / ОТКРЫТО / не проверено). Составлена по замечанию заказчика: анализ до аварии, а не после. Открытые зоны на сегодня: судья для невизуального; второй этап целиком; масштаб плана (300 задач против проверенных 46); смешанный класс артефакта; суждение о выполнимости на входе; читаемость отчёта человеком. Нашёл новый режим отказа — дописывай строку туда же, это живой документ.

**Второй этап принят в план вехой (А-41в), не в этой сессии**: работа над СУЩЕСТВУЮЩИМ проектом — указать директорию, поднять состояние, продолжить план, дополнить новой задумкой, память решений между заходами. D-324/D-325/D-326 были его ранними симптомами; учитывай это, когда правишь ключи в п.3 — схема должна пережить второй заход в тот же проект.

## Не трогать

Прежний перечень целиком. Бенчмарк-артефакт `tg-20260824-002500` сохранить уликой — не пересобирать, не полировать. NEURA сохранить. Задачи 177–180 и финализация — после п.1. Учётки не печатать. Приёмка продукта — за заказчиком; артефакт за принятый не выдавать.
