# START_HERE — точка входа исполнителя

> Пишет Архитектор. Фраза заказчика «Прочитай .specs/handoff/START_HERE.md и исполни» = вердикт ниже утверждён.

**Сессия:** M13п · раунд 2 — задачи 143, 147, 144 (шов при нехватке контекста — после 144) · **Дата:** 2026-08-18 · **Режим сессии: ULTRACODE** (приказ заказчика на весь milestone; ширину — в русскую копию 143, ансамбль 144 и, в следующем раунде, в 145/146; механика обычным темпом)

## Вердикт по раунду 1: задача 142 ПРИНЯТА, все три вопроса §8 решены (А-17)

Диагноз Raw одним замером (правый край 28 516 px), supersededBy как собственный факт проекции с обходом ловушки «я не хвост», считаемое правило главного действия через `data-variant`, и разбор трёх красных CI до внуков `apt-get`, держащих lock, — принято целиком, D-205…D-213 в журнале. Сдвиг шва — подтверждён: твои цифры (551 место, 80 текстовых утверждений, три в общей фикстуре) — это порядок работ, а не оправдание, и порядок ты назвал верный.

**Решения по твоим вопросам:**

1. **Локаль живёт в cookie** (читается сервером; ключ объявлен в `UI_STATE_KEYS`). Твой анализ верен: различие локали — сам текст, клиентское хранилище даёт мигание или рассогласование гидратации на каждой строке. Запиши D-строкой.
2. **Raw: паритет с эталоном — переносить.** Мой AC был написан до видео; видео право. Длинная строка продолжается следующей визуальной строкой без номера (номера — у логических строк), горизонтальной прокрутки во вьюере нет нигде. Побайтовость Copy/Download (задача 122) не тронута — это правка отображения.
3. **Предпросмотр — оверлей по видео**: оформлено задачей **147** (читай AC в tasks.md) — центрированный модал, слева имя файла + Revision N, справа одной линией Outline · вкладки · Copy · Download · ✕, одна ширина у всех вкладок, Outline — выпадающий у своей кнопки. Docked-панель уходит; Esc/✕ закрывают; ливность над живым стримом — перепроверить (Stop достижим).

## Порядок раунда 2

1. **143 — RU-локаль, строго твоим порядком из §5:** (а) снять зависимость тестов от английского текста отдельным зелёным коммитом (фикстура journey — первой); (б) реестр строк + линт «литерал = ошибка» на полном дереве; (в) рукописная русская копия ансамблевого качества — множественные формы (одна/две/пять) обязательны, «предложения из JSX-осколков» переписываются под перевод целыми фразами; (г) флаг умолчания `ru` — последним, на зелёном наборе. Словари методологий локализуются, не дублируются.
2. **147 — оверлей и перенос Raw** (после 143, чтобы копия оверлея сразу родилась двуязычной).
3. **144 — режим «Конкретный»** с уточнением из видео: справка — свойство **опции**, не вопроса: логотип технологии, иконка ⓘ, внешняя ссылка ↗, тег-чипы — только на опциях, называющих реальную технологию; `No preference`/`Other`/нетехнические — без обвеса. Ансамбль промптов и панель судей — как в задании раунда 1.
4. Шов при нехватке контекста — после 144; задачи 145 (автономный субъект) и 146 (гейт) — раунд 3.

## Не трогать

Прежний перечень в силе. Про Raw: правка отображения не должна коснуться ни одного байта контента — тест «Copy равен выгрузке» обязан остаться зелёным без изменений. Рапорт — «Раунд 2» сверху в REPORT-M13.md, две строки в чат.
