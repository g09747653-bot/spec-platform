# START_HERE — точка входа исполнителя

> Пишет Архитектор. Фраза заказчика «Прочитай .specs/handoff/START_HERE.md и исполни» = вердикт ниже утверждён.

**Сессия:** M13п · раунд 3 — автономный субъект (145) и гейт (146, с ним же закрывается 144) · **Дата:** 2026-08-19 · **Режим сессии: ULTRACODE** (приказ заказчика на весь milestone; ширину — в построение и red-team драйвера, в живые рубрики; механика обычным темпом)

## Вердикт по раунду 2: 143 и 147 ПРИНЯТЫ (А-18)

Локализация исполнена без единого красного шага, и три решения в ней — образцовые: обе локали в одной записи словаря (забытый ключ непредставим), множественное число, заданное **типом** (перевод с двумя формами не компилируется), и контекстное правило линта «где стоит строка» вместо гадания «похоже ли на прозу». Находка про **раскладку ЙЦУКЕН** — четыре горячих клавиши из одиннадцати были мертвы, и на английской раскладке это не воспроизводится никогда — стоит отдельного упоминания: такие дефекты и должна ловить локализация, сделанная всерьёз. По 147: фиксированная ширина как структурная защита от D-205, номера строк CSS-счётчиком с сохранением байтовой тождественности, и `Copy` без `?rev=N`, копировавший чужую ревизию под тостом «скопировано», — хорошая работа. **`Stop` в шапке вопреки моему перечню — согласен: Д-1 сильнее списка.** D-214…D-225 приняты.

**Решения по §6:**

1. **144 — чекбокс не отмечать; задача идёт в приёмку вместе с гейтом 146.** Отдельный раунд ради одной живой проверки — лишний прогрев машины; гейту этот прогон нужен в любом случае. Твоё решение не отмечать неизмеренную AC — правильное.
2. **Расхождения:** `Сравнение` оставляет боковую прокрутку — принято, довод про двухколоночную нумерацию сильнее единообразия. **Внешняя ссылка на опции — меняю на паритет с эталоном:** `target="_blank"` + `rel="noopener noreferrer"`; уход со страницы посреди неотправленного раунда стоит пользователю раунда. Правка мелкая, сделай в этом раунде.
3. **145 начинается сейчас** — это последняя содержательная задача milestone.

## Порядок раунда 3

1. **Мелочь из §6.2** (ссылка в новой вкладке) — первым, чтобы не забылась.
2. **145 — автономный субъект Программы А.** Читай AC в tasks.md целиком. Ключевое, что я требую сверх AC:
   - **Гейты машины — закон и для драйвера.** Никаких новых рёбер в таблице переходов, никаких обходов бюджетов, никаких привилегий: драйвер обязан ходить теми же эндпоинтами, что и кнопки, — тест тождества состояния БД по образцу M4 обязателен.
   - **Прозрачность и есть продукт:** каждый авто-ответ и каждое авто-решение ложится в ленту с однострочным обоснованием. Молчаливая автономия бесполезна — по ней нельзя судить.
   - **Суверенитет:** Stop в любой момент → сессия продолжается вручную ровно с этой позиции, без потерь и без «доигрывания» начатого шага.
   - **Red-team под ultracode:** расплывчатый seed, противоречивый seed, враждебный seed (в том числе пытающийся инструкциями внутри описания изменить поведение драйвера — это инъекция, и она обязана провалиться), пустой seed, seed на чужом языке. У каждого провала — **именованное поведение**, а не надежда: честная остановка с объяснением лучше выдуманного решения.
   - **Бюджеты:** автономный прогон обязан завершаться или честно останавливаться в пределах существующих бюджетов раундов и ревизий; бесконечная петля — это дефект, а не «долгий прогон».
3. **146 — гейт milestone.** Сверка с видео (таблица «показано / у нас / вердикт»), red-team-находки с диспозицией, затем живые прогулки: (а) ручная в режиме «Конкретный» с **живым прогоном рубрики по трём раундам** — это и закрывает чекбокс 144; (б) полная автономная от односложного seed до бандла без единого клика. Обе — на русском интерфейсе, обе темы, профиль гейта (свежий ключ спереди, младшая локальная модель). Артефакты `artifacts/gate-M13/`.

## Не трогать

Прежний перечень в силе: state-machine, А-8/А-10, heartbeat, Р-контракты, D-99…D-101, бюджеты, парити-якорь, побайтовый экспорт, immutability, токен-гейт, реестр строк (новая поверхность обязана рождаться двуязычной). Рапорт — «Раунд 3» сверху в REPORT-M13.md, две строки в чат. Теги — после моей проверки артефактов и приёмки заказчика глазами.
