# START_HERE — точка входа исполнителя

> Пишет Архитектор. Фраза заказчика «Прочитай .specs/handoff/START_HERE.md и исполни» = вердикт ниже утверждён.

**Сессия:** M9п · раунд 3 — разблокировка гейта 123 · **Дата:** 2026-08-16 · **Режим сессии: Opus high** · Раунд короткий: только модель + перезапуск гейта

## Вердикт по раунду 2: работа принята, блокировка признана внешней

Задачи 118–122 приняты (проверены рапорт, PR #41, решения D-130…D-143; разбор `= ANY($1)` и DEFERRABLE-ключей — зачёт; «атомарно = одним statement'ом» с индуцированным сбоем — ровно тот тест, который нужен). BLOCKED по 123 оформлен правильно: обе неудачи в цепочке, продукт вёл себя верно, артефакты доказательны.

**Решение заказчика по §6: гейт гоняем на локальной модели Qwen 3.8 27B.** Запиши это D-строкой («по решению заказчика, 2026-08-16»).

## Порядок раунда

1. **Найди и подтяни модель.** Заказчик назвал её «qwen 3.8 27b» — найди точный тег в реестре Ollama (`ollama pull qwen3.8:27b` или ближайший официальный тег этого семейства/размера; если квантов несколько — бери стандартный q4_K_M). Если модели с таким именем/размером в реестре нет — возьми ближайшую по семейству и размеру, назови выбор и причину в рапорте, не останавливайся.
2. **Предполётный замер по образцу D-91, прежде чем жечь полный гейт.** Один прогон на собственном промпте репозитория для **SpecKit-конституции** (та самая, что дважды валила гейт: обязательные секции `## Core Principles`, `## Governance` и остальные из вендоренного шаблона): все секции на месте + время до первого токена и полное. Помни про VRAM: 27B на 16GB пойдёт с частичной выгрузкой в RAM — это ок для гейта, если полный документ укладывается в провайдерский таймаут 300 с. Замер — в decisions.md.
   - **Fallback, если 27B непрактична** (не тянет таймаут или не существует): `qwen3:14b` — у неё доказанная структура из D-91; отметь в рапорте, что сработал fallback, и продолжай с ней. Второй раз к заказчику с этим вопросом не ходим.
3. **`OLLAMA_CONTEXT_LENGTH=16384` обязателен как был (D-92)**; проверь `ollama ps` после первого вызова — 27B с урезанным контекстом повторит историю D-92 в худшем виде.
4. **Перезапуск гейта 123 после сброса дневной квоты Google** (цепочка `google,ollama` — свежие 20 вызовов уйдут на самые ответственные генерации, хвост держит новая локальная). Повторы D-142 и пересэмпл D-143 остаются как есть. Живой набор — прежний (SpecKit полный + brownfield короткий + Edit + смена модели), сверх него ничего.
5. Артефакты — `artifacts/gate-M9/run-3/`; RESULT фиксирует счётчики линтеров на requirements/tasks досках (AC-3 задачи 123 — в этот раз прогулка обязана до них дойти).
6. Рапорт «Раунд 3» сверху в REPORT-M9.md: замер модели, вердикт гейта, ссылка на артефакты. Зелёный гейт = M9п готов к приёмке; жди вердикта, **M10п не начинай**.

## Не трогать

Продукт под гейт не подгибается: структурная проверка, Р-1/Р-2/Р-3, D-99…D-101, парити-якорь, побайтовый экспорт — без изменений. Меняется ровно одна переменная модели гейта (+ D-строка) — больше ни одной строки продукта.
