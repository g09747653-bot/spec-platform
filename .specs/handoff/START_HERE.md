# START_HERE — точка входа исполнителя

> Пишет Архитектор. Фраза заказчика «Прочитай .specs/handoff/START_HERE.md и исполни» = вердикт ниже утверждён, включая приёмку заказчика глазами.

**Сессия:** M16а — параллельность и «Красный CI» (Программа А) · **Дата:** 2026-08-20 · **Режим сессии: MAX**

## Вердикт по раунду 2: M15а ПРИНЯТ ЦЕЛИКОМ (А-25)

Гейт 158 зачтён; milestone закрыт. Поимённо отмечено: подписочный путь ИЗМЕРЕН, а не предположен, — таблица «по одному флагу за раз» против враждебной директории это образец того, как надо устанавливать границы изоляции; «ровно один из двух» с отказом при обеих учётках — правильное чтение приоритета CLI; смерть `cpSync` на симлинках поднята до причины с тремя замеренными вариантами и решена там, где симлинки родились; чужой объём (словарь шума платформенного набора) назван прямо и легализован — красный CI блокировал любой PR, а вердикт подброшенной монеты не вердикт. Отдельно — report_1.json: контур поймал галлюцинацию собственного бандла руками живого исполнителя (`yoctocolor` → `yoctocolors`) и записал решение. Это первый содержательный акт самокоррекции контура. D-263…D-276 приняты.

Ответы на §7 — все пять решены (полные формулировки в А-25): (1) интейк станет идемпотентным по прозе — задача 172; (2) мёртвое умолчание модели сменить — в объёме 172; детерминированный фолбэк финального прогона — законный исход D-229, модельный путь перепроверит гейт 163; (3) `--allowedTools` достаточно, денай-лист не заводить; (4) потолок 5 минут не двигать — гейт 163 собирает распределение, решаем по данным; (5) `loop:e2e` — в существующую джобу, задача 173.

## Порядок M16а

1. **Теги первым действием** (после приёмки заказчика глазами, которая происходит его командой на эту сессию): `m15a-accepted` на смерженный main.
2. **Долги: 170 → 171 → 172 → 173.** Все четыре Small/Medium, все разблокируют ядро: 170 снимает стояние драйвера после рестарта (улика `.local/db-m15a-stalled-040556`, хранится), 171 делает прогулку M14а перезапускаемой, 172 защищает прозу заданий от возобновления, 173 ставит браузерный набор контура под CI.
3. **Ядро: 159 → 160 → 161 → 162 → 163.** Полные тела в tasks.md. Сверх написанного требую:
   - **Троттлинг — штатный режим.** `rate_limit_event` не-`allowed` — это пауза с объявлением в ленте, не ошибка; каскадный отказ на троттлинге — красное условие гейта. Штатно на подписке, все возможности (А-24 п.2).
   - **Заморозка — свойство диска.** `PAUSED` переживает рестарт и не самовозобновляется; единственный путь назад — retry. Тест обязателен.
   - **Per-role провайдер** с умолчанием «та же учётка, что исполнитель» (А-24 п.1); переключение — конфигом, тест обязателен.
   - **Гейт 163 — на бандле с настоящей параллелью**: нужна веха с ≥3 непересекающимися задачами; если текущий бандл такой не даёт — новый автономный прогон платформы с seed под несколько независимых модулей (профиль гейта платформы: свежий ключ спереди; урок квоты ~35 вызовов — не жечь её до прогулки).
   - **Замеры гейта — данные решений**: длительности итераций (потолок 5 мин), rate_limit-события, фактическая параллель, VRAM-пик. Всё в артефакты.
4. **Рапорт** — секция сверху в REPORT-M16A.md (новый файл), две строки в чат.

## Не трогать

Прежний перечень платформы и пакета в силе. Скелет M17а (164–167) не начинать. Backlog сверх названного не реализовывать. Улика `.local/db-m15a-stalled-040556` — хранить до закрытия 170. Значения учёток не печатать нигде; красное условие на утечку — в каждом live-скрипте контура. Теги — по п.1; финальная приёмка milestone — моя проверка артефактов, затем заказчик глазами.
