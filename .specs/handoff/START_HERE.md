# START_HERE — точка входа исполнителя

> Пишет Архитектор. Фраза заказчика «Прочитай .specs/handoff/START_HERE.md и исполни» = вердикт ниже утверждён.

**Сессия:** M7п · Chat-first ядро (задачи 104–110) · **Дата:** 2026-08-15 · **Режим сессии: Opus MAX**

## Вердикт по M6 раунд 5: ПРИНЯТ. M6 закрыт

Диагноз Р-3 по базе заказчика вместо догадки, каркас `session-request` с тремя свойствами, исчерпывающие карты reason-кодов, liveness внутри полёта, самопроходимый гейт GREEN на 56 состояниях — и находка дубля генерации с тремя слоями фикса (D-99…D-101, особенно корень D-101 про `restarted`) — образцовый раунд. Проверено мной по коду, артефактам и PR #36. Отступление по trace/video принято: 56 скриншотов + лог контролов — достаточная доказательная база; лёгкий trace со следующего прогона — верное решение.

**Первым действием сессии:** поставь аннотированный тег `m6-accepted` на текущий `main` и запушь его. Этап 1 заказчика (MVP-копия) закрыт.

## Что читать перед работой (в этом порядке)

1. `constitution.md` (как всегда) + хвост `decisions.md` (амендменты А-2/А-2.1/А-3, North Star).
2. `execution.md` §3-bis — парити-программа, гейты и режимы.
3. **`.specs/research/myspec-parity-reference.md` — эталон приёмки.** Части 1.1–1.3 — обязательно и внимательно: это наблюдаемое поведение оригинала, против которого принимается каждый парити-гейт. Части 5–6 — контекст (документация оригинала и граница «сырьё/разработка»).
4. `tasks.md`, раздел «Parity Programme» → **Milestone 7п, задачи 104–110**. Старые блоки Milestone 7–9 помечены DEFERRED — их не трогать.

## Объём сессии: только M7п (104 → 110 по порядку)

Суть: вся сессия переезжает в единую ленту чата — сообщения, анкеты-раунды, стадийные чипы, карточки документов — поверх нетронутых контрактов состояния, стриминга и живости.

Особые указания Архитектора:

1. **Задача 104 — фундамент milestone, максимальная тщательность.** Лента = чистая проекция персистентного состояния, без нового write-path. Если при проектировании увидишь, что какое-то состояние M6-гейта не выражается проекцией — это уровень 2: остановись и спроси, не изобретай запись.
2. **Контракты не трогаются:** Р-1 (парсер+повтор), Р-2 (cancel пуст), Р-3 (`session-request`, gate-copy), D-99…D-101 (reattach, один прогон), матрица переходов M2. Лента — новый рендер тех же фактов.
3. **Схема анкет v3 (задача 106) обратно совместима с v2** — старые сессии заказчика обязаны рендериться. Тест на v2-фикстуре обязателен.
4. **У-1 (задача 108):** инструкция языка — из одной точки сборки промптов. Появление второй копии инструкции в отдельном промпте — красный флаг для саморевью.
5. Селекторы e2e при переезде меняй через testid, не через текст — тексты станут локализуемыми после У-1.
6. Гейт M7п (задача 110) проходишь сам по А-2.1: артефакты в `artifacts/gate-M7/`, лёгкий trace коммитится, видео нет. Google может весь день отвечать 429 — прогон на ollama-цепочке легитимен (решение заказчика, decisions.md).

## Стоп-правила

Прежние: одна сессия = один milestone; красный CI = стоп; 2 подхода → BLOCKED; идеи «заодно» → Backlog. **M8п не начинай.** Рапорт — новой секцией сверху в `.specs/handoff/REPORT-M7.md` (новый файл), в чат заказчику — две строки по протоколу.
