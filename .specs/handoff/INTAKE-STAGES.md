## Разбивка интейка по стадиям и цена веера (А-51 п.3)

Бандл: `C:\Users\Bob\Desktop\F\Fmyspec-clone-spec-platform\artifacts\gate-M14a\machine-bundle\bundle`. Задержка модельного звена: **9098 мс на вызов** —
не выдумана, а взята из живого замера А-44 (`intakeMs = 127369` при 14 вызовах широкого прогона).

Настоящее здесь всё, кроме задержки звена: те же стадии, тот же порядок, тот же код. Числа не
предсказывают живой интейк на другой цепочке — они называют, какая доля стены на какой стадии
лежит и что из этого забирает веер.

### Потолок веера: 1

- задач в плане: 16; заданий написала модель: 16
- стена интейка: **182.2 с**

| стадия | стена | доля | модельных вызовов |
|---|---|---|---|
| исследование | 9.1 с | 5.0% | 1 |
| класс задумки | 9.1 с | 5.0% | 1 |
| выполнимость | 9.1 с | 5.0% | 1 |
| объём | 9.1 с | 5.0% | 1 |
| задания | 145.7 с | 79.9% | 16 |
| **интейк целиком** | **182.2 с** | 100% | **20** |

### Потолок веера: 5

- задач в плане: 16; заданий написала модель: 16
- стена интейка: **73.0 с**

| стадия | стена | доля | модельных вызовов |
|---|---|---|---|
| исследование | 9.1 с | 12.5% | 1 |
| класс задумки | 9.1 с | 12.5% | 1 |
| выполнимость | 9.1 с | 12.5% | 1 |
| объём | 9.1 с | 12.5% | 1 |
| задания | 36.4 с | 49.9% | 16 |
| **интейк целиком** | **73.0 с** | 100% | **20** |

### Итог

- стена интейка: 182.2 с → 73.0 с
- **ускорение интейка: 2.50×**
- стадия заданий: 145.7 с → 36.4 с (**4.00×**) — именно она и параллелится
- остальные стадии не тронуты: они последовательны по существу (класс нужен плану, выполнимость и объём нужны условиям), и веер им не положен

## Живой прогон (А-52 п.2) — вторая таблица, настоящая цепочка

Тот же бандл (`artifacts/gate-M14a/machine-bundle/bundle`), тот же код, те же два потолка веера;
задержки не подставлены — каждый вызов был настоящим ходом цепочки контура
(`LOOP_PROVIDER_ORDER=claude-cli,google`; все 33 хода обоих прогонов принял мост подписки, google
не понадобился). Инструмент — тот же CLI с флагом `--live` (D-342); сырец — `INTAKE-STAGES-LIVE.md`
и `INTAKE-STAGES-LIVE.json` рядом.

### Потолок веера: 1 (живой)

- задач в плане: 12; заданий написала модель: 12
- стена интейка: **636.4 с**

| стадия | стена | доля | модельных вызовов |
|---|---|---|---|
| исследование | 7.1 с | 1.1% | 1 |
| класс задумки | 5.5 с | 0.9% | 1 |
| выполнимость | 21.8 с | 3.4% | 1 |
| объём | 23.9 с | 3.8% | 1 |
| задания | 578.1 с | 90.8% | 12 |
| **интейк целиком** | **636.4 с** | 100% | **16** |

### Потолок веера: 5 (живой)

- задач в плане: 13; заданий написала модель: 9 (плюс 4 деградации — вызов был, ответ не разобрался, задание написано заготовкой; колонка «вызовов» считает разобранные ответы, сетевых ходов заданий было 13)
- стена интейка: **282.2 с**

| стадия | стена | доля | модельных вызовов |
|---|---|---|---|
| исследование | 6.9 с | 2.5% | 1 |
| класс задумки | 6.2 с | 2.2% | 1 |
| выполнимость | 16.9 с | 6.0% | 1 |
| объём | 19.5 с | 6.9% | 1 |
| задания | 232.6 с | 82.4% | 9 |
| **интейк целиком** | **282.2 с** | 100% | **13** |

### Расхождение с симуляцией — числом

| величина | симуляция | живьём | расхождение |
|---|---|---|---|
| задержка звена (посл., средняя на вызов) | 9.1 с (из А-44) | 39.8 с | **×4.37** |
| стена интейка, потолок 1 | 182.2 с | 636.4 с | ×3.49 |
| стена интейка, потолок 5 | 73.0 с | 282.2 с | ×3.87 |
| **ускорение интейка веером** | **2.50×** | **2.25×** | −10% |
| ускорение стадии заданий | 4.00× | 2.49× | −38% |
| доля стадии заданий (посл.) | 79.9% | 90.8% | +10.9 п.п. |
| средний ход задания: последовательно | 9.1 с | 48.2 с | — |
| средний ход задания: в веере | 9.1 с | 60.4 с (макс 147.8) | **веер удорожает ход на ~25%** |

Что из этого следует, и что числа НЕ значат:

- **Веер живьём подтверждён**: пять одновременных запросов заданий видны в журнале моста одной
  секундой (11:09:13), ускорение интейка 2.25× против обещанных симуляцией 2.50× — строение
  разбивки совпало, доля стадии заданий доминирует в обеих.
- **Почему стадия дала 2.49× вместо 4.00×**: (а) одновременные ходы моста ДОРОЖЕ по одному —
  средний веерный ход 60.4 с против 48.2 с последовательного, хвост до 147.8 с: подписочный CLI
  делит канал, чего симуляция с фиксированной задержкой не видит по построению; (б) хвост волн:
  13 заданий при потолке 5 — последняя волна из трёх.
- **Живой объём РЕЖЕТ этот план**: суждение об объёме на настоящих оценках модели срезало
  16 задач до 12 (потолок 1) и до 13 (потолок 5) при бюджете 44 единицы — стадия, которая в
  симуляции была бесплатной формальностью (все цены = 1), живьём работает и работает недетерминированно:
  два прогона — два разных сокращения. Сравнение стен поэтому сравнивает немного разные объёмы
  работы; по-задачная арифметика: 48.2 с/задание последовательно против 232.6 с/13 заданий веером.
- Задержка звена этого часа (39.8 с/вызов) в 4.4 раза выше замеренной в А-44 (9.1 с) — мост
  один и тот же, час другой; абсолютные стены живого прогона предсказаниями не являются.
