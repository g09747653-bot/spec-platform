# REPORT-M10 — рапорты исполнителя по Milestone 10п

> Новые секции добавляются **сверху**. Точка входа сессии — `.specs/handoff/START_HERE.md`.

## Раунд 2 — 2026-08-17 · M10п финал (задачи 131, 128, 129)

**Статус: три задачи сданы, гейт зелёный, milestone закрыт и ждёт приёмки. PR #49 (131) смержен,
PR #50 (128+129) открыт и в прогоне. Гейтовая прогулка — GREEN: 51 состояние, ноль проблем, ноль
обрезок, ноль структурных отказов, ноль ошибок консоли. Теги `m10p-accepted` и `stage1-complete` не
ставил — они за вами.**

Два результата этого раунда важнее самих задач, и оба неприятные в правильную сторону.
**Первый:** ultracode-прогон нашёл, что мой парити-чек-лист был слишком щедрым — двадцать с лишним
«парити» держались на присутствии `data-testid`, а не на описанном поведении. **Второй:** предполёт
показал, что ограниченная генерация (А-10) сегодня не отделяет успех от провала — на гейтовой модели
и без неё три из трёх. Причина названа ниже, и она не отменяет вердикт, но меняет то, что им
доказано.

### 1. Задача 131 — ограниченная генерация у локального адаптера

Вердикт А-10 исполнен как написан: три названных артефакта (Edit-предложение, review.v2, черновики
интервью) вызываются со схемно-ограниченной генерацией.

- **Только провайдер по адресу, и на вендорной кромке** (D-172). Тот же предикат, что у
  `generationBound`: модель по адресу — это рантайм, которому можно запретить сэмплировать вне
  грамматики. `responseFormat` у SDK доезжает до Ollama как `response_format: {type: "json_schema"}`
  и применяется как её родное `format`. Через `streamText` этого не выразить, поэтому модель
  оборачивается `wrapLanguageModel` + `defaultSettingsMiddleware` — то есть **значением по
  умолчанию**, которое будущий вызов со своим форматом не потеряет.
- **Облачный путь не изменился побайтово, и это проверено телом запроса.** Тест подменяет `fetch`,
  даёт настоящему SDK собрать настоящий запрос и сравнивает строки с заявленной схемой и без неё.
  Локальное тело отличается ровно одним ключом. Снапшот через мок SDK доказал бы меньше.
- **Схема выводится из Zod-схемы, которая ответ валидирует** (D-173), с `io: 'input'` — поле с
  `default` обязательно на выходе и необязательно на входе, а вход пишет модель. Страж от
  расхождения сравнивает список обязательных полей с ответом самой Zod (и считает **все** коды
  ошибок: отсутствующий enum отчитывается как `invalid_value`, и фильтр по `invalid_type` молча
  вынес бы `stage` и `verdict` из сравнения).
- **Ремонт рекомендаций пишет строку** (D-174) — со счётчиками, только когда действительно снял флаг,
  и молчит, когда сработал по другой причине.

**Предполёт (AC-1), и здесь нужна ваша реакция.** Состояние D-161 воспроизведено из транскрипта
самой упавшей прогулки: четыре её документа, та же инструкция, промпт **7 761** токен против 7 596 в
день находки. Ограниченные прогоны — **три из трёх** пригодных предложений на четыре файла с первой
выборки, ноль обрезок. **Но неограниченные — тоже три из трёх.** Причина: `qwen3:14b`, на котором
D-161 был найден, сегодня на этой машине не работает — 11,1 ГиБ весов против ~9,6 ГиБ свободной
VRAM, драйвер выгружает, **0,65 ток/с против 68** у `qwen3:8b`. Поэтому доказано «ограниченный вызов
надёжен на гейтовой модели», а не «ограничение чинит D-161»: второе на этой машине непроверяемо
(D-175). Обе половины замера в артефакте, инструмент — `e2e/edit-preflight.preflight.ts`.

### 2. Задача 128 — чек-лист и red-team

Чек-лист построен из §1.1–1.5 и пройден по пунктам `e2e/parity-M10.spec.ts` на стабе. Затем — один
прогон, как задано: шесть линз, независимый скептик на каждую (его задача — **опровергнуть**, по
умолчанию «опровергнуто» при сомнении), критик полноты в конце. 13 агентов, 40 минут, 71 находка.

**Он поправил меня, а не продукт, и это его главный результат.** Мои «парити» слишком часто означали
«testid на месте»: наличие кнопки — не её оформление, счёт элементов на странице — не их место в
композере, атрибут, снятый в одну секунду, — не его устойчивость. Что нашлось:

- у пузыря пользователя **нет приглушённой заливки** — он байт в байт цвета полотна, а токен под эту
  роль в системе есть и применён один раз в hover переключателя темы;
- у стадийного чипа из четырёх названных эталоном признаков совпадает **один**;
- **панель завершения — не финал ленты**: под ней рендерятся бар стадии и блок Refine, а реплики
  чата приклеиваются ещё ниже — и с перештамповкой позиции на текущую, что противоречит докблоку
  нашего же `feed-item.tsx`;
- в **композере нет вложения** (контрол в сайдбаре, а прогулка считала его по всей странице) и нет
  брендового градиента ни на одной кнопке приложения;
- ручка сайдбара **сжимает, но не расширяет**;
- имена методологии не доходят до кнопки перехода, чипа и подписей карточек — на одном экране два
  словаря; бюджет раундов методологии до поверхности не доходит вовсе (`roundBudgetFor` не вызвана
  ниоткуда — ровно дефект D-97).

**Итог: 17 парити, 21 своё отличие, 22 разрыва** (4 содержательных, 11 поверхностных, 7
косметических; одиннадцать строк несут двойной вердикт — половина совпала, половина нет). Все 71
находка с диспозицией в `RED-TEAM.md`. Арифметику пересчитал из самих таблиц, строка за строкой: в
первой редакции итог был написан отдельно от данных и не сходился — это тоже нашёл прогон.

**Ни один разрыв не чинится в 129, и это вывод, а не уклонение.** Каждый меняет либо контракт (блок
ленты, схема набора вопросов, поверхность методологии), либо оформление, задаваемое токенами и
компонентами; двадцать два таких пункта — объём милестоуна, а не гейтовой прогулки, и правило 5 прямо
запрещает мне брать это заодно. Шесть дешёвых правок выделены списком, чтобы вы могли назначить их
одной строкой.

### 3. Задача 129 — гейт

**Предполёт по вашему профилю.** `qwen3:8b` проходит все три части: структурно верная конституция на
упакованном состоянии run-2 за 23,3 с при нуле обрезок (контрольный неупакованный прогон рядом
воспроизводит отказ — обе половины измерены), три пригодных JSON-раунда из трёх с первой выборки без
ремонта, три ограниченных Edit-предложения из трёх. Взял её, как предписано (D-176).

**Прогулка — GREEN.** Полный путь на методологии эталона, одна стадия отправлена на доработку (Rev 2
и повторная доска — цикл ревью пройден вживую, а не заявлен), панель завершения и промпт, называющий
**все четыре** одобренные ревизии, обе темы прочитаны на живых состояниях (`lab(98.4…)` против
`lab(4.4…)` — токены действительно переключаются), «go back» дописал Rev 3. Ноль обрезок, ноль
структурных отказов — второе я добавил новым красным условием гейта.

**Четыре прогулки до неё были красными, и все четыре — мой инструмент, не продукт** (`RUNS.md`):
читал `innerText` у `<textarea>`, спрашивал «go back» в позиции, где файла ещё нет, ждал карточку
отката для файла, которого сессия не касалась последним. Тот же класс ошибки, что red-team нашёл в
чек-листе, и он стоил четырёх прогулок по восемь минут. Скриншоты красных прогонов удалил (115 МБ
картинок ни о чём), рапорты и консоли оставил.

### 4. Тесты и CI

| Что | Результат |
|---|---|
| `pnpm test:unit` | 95 файлов, **1830** тестов — зелено |
| `pnpm test:boundaries` / `lint` / `typecheck` / `format:check` | зелено |
| CI PR #49 (задача 131) | зелёный, смержен |
| CI PR #50 (задачи 128+129) | в прогоне |
| Живой гейт | **GREEN**, `artifacts/gate-M10/RESULT.md` |

Новые наборы: `structured-output.test.ts` (5, тело запроса на проводе), `constrained-output.test.ts`
(5, страж от расхождения схем), два кейса на строку ремонта. Новые инструменты:
`e2e/edit-preflight.preflight.ts`, `e2e/round-preflight.preflight.ts`, `e2e/parity-M10.spec.ts`,
`e2e/gate-M10.live.ts`.

### 5. DECISIONS

D-172…D-175 (задача 131), D-176 (гейтовая модель). В Backlog — два соседа Edit по контракту
(refinement и reply-assessment разбираются тем же `parseJsonDocument`; в списке А-10 их нет, поэтому
не тронуты) и потолок в 300 с у глобального `fetch` Node, когда локальный рантайм молча читает
длинный промпт.

### 6. Что нужно от Архитектора

1. **22 разрыва парити с владельцами** (`artifacts/parity-M10/CHECKLIST.md`, раздел «Разрывы, по
   весу»). Четыре содержательных — аналитический мостик между раундами, перештамповка позиции у
   реплик чата, два словаря названий методологии, бюджет раундов мимо поверхности. Ни один не мой
   уровень: каждый меняет контракт.
2. **21 «своё отличие»** — подтвердить списком или вернуть какие-то в разрывы. Одно уже
   подтверждено вами (баннер вместо модалки).
3. **Вердикт по D-175:** А-10 исполнен, но на гейтовой модели ограничение сегодня не является тем,
   что отделяет успех от провала. Оставляем как гарантию (моё мнение — да: грамматика делает
   разбираемость свойством сэмплера, а не послушности) или снимаем.
4. **Теги** `m10p-accepted` и `stage1-complete` — по вашей приёмке.

### 7. Следующий шаг

Стоп по правилу 7. Программу А не начинал.

## Раунд 1 — 2026-08-17 · M10п (задачи 124, 125, 126, 127)

**Статус: четыре задачи визуального слоя сданы, шов пройден — остановка на месте, которое вы
и назвали естественным (после 127, перед 131). Тег `m9p-accepted` поставлен первым действием и
запушен. Каждая задача — отдельный PR: #45 (124) и #46 (125) зелёные и смержены, #47 (126) и
#48 (127) — следом, по мере зелёного CI. Локально зелено всё: 1818 юнит-тестов, 80 e2e на
Chromium, новые спеки прогнаны дополнительно на WebKit.**

Найдено по дороге две вещи, о которых стоит знать до гейта 129, и обе — дефекты нашего
продукта, а не косметика: `loading.tsx` превращал 404 чужой сессии в мягкий 200 (D-167), и
баннер «Connection lost» в сценарии session-request живёт мгновение, потому что каждый такой
запрос перечитывает страницу (D-165). Первое починено, второе — названное свойство, а не
недоделка; подробности ниже.

### 1. Задача 124 — токены и тема со своим оттенком

**Свой оттенок буквально.** Структура токенов — по эталону (`background`/`surface`/`foreground`/
`primary` + шкала `text-h1…h3/body/caption/label`); палитра наша: **азур 245° и золото 78°**.
Их бренд — фиолетово-циановый (282°/195°/303°); расстояние по тону утверждается тестом (≥25°),
а не обещанием в комментарии.

Что важно в этой задаче помимо цветов:

- **Одно место для цвета — это lint, а не договорённость.** До задачи цвета жили в двух:
  восемь переменных в `globals.css` **плюс 33 палитровых класса Tailwind прямо в компонентах**
  (`text-red-700`, `bg-slate-50`). Второе множество и есть дефект, который тема обнажает: такой
  класс рисует один и тот же цвет в обеих темах и выглядит правильным ровно до переключения.
  Правило `design-tokens/no-raw-colours` роняет сборку, у него три фикстуры (две отвергаемые,
  одна разрешающая), и `pnpm test:boundaries` падает, если правило перестало кусаться —
  прецедент границ модулей. CSS-половину («ни один стилевой файл, кроме бренда, не называет
  цвет») закрывает `brand.test.ts`: ESLint не разбирает CSS (D-162).
- **Контраст измеряется, а не смотрится.** `brand.test.ts` читает `src/app/brand.css`,
  конвертирует OKLCH → sRGB так, как это делает дисплей (гамма, отсечение по кубу, обратная
  линеаризация), и утверждает WCAG AA по **25 парам в обеих темах**. Палитра подгонялась под
  тест: `border-strong` и `foreground-subtle` двигались до прохода, а не наоборот (D-163).
- **Тема — не состояние React.** Она живёт на `<html>`, куда её ставит скрипт в `<head>` до
  первой отрисовки, и в `localStorage`. `useSyncExternalStore` читает атрибут; зеркалить его в
  `useState` значит завести второй источник правды — на первой версии это и поймало правило
  `react-hooks/set-state-in-effect`, и оно было право (D-164).
- Переименование 206 старых утилит (`ink`→`foreground`, `accent`→`primary`, `canvas`→`background`)
  механическое, поведение не менялось.

**AC:** переключатель переживает перезагрузку (`e2e/theme.spec.ts`, Chromium + WebKit); гейт на
захардкоженные цвета — есть и с зубами; контраст — 58 утверждений; e2e-набор зелёный, тёмная
тема прогоняется отдельным смоук-проходом стадии.

### 2. Задача 125 — лоадер, «Connection lost», тосты

**Дефект, который нашла эта задача, важнее самой задачи.** Лоадер сессии, поставленный как
`loading.tsx` (файловая конвенция Next), **сломал 404**: фолбэк рендерится над всем сегментом,
включая `notFound()`, тело ответа начинает течь, и статус после этого сменить нельзя. Сессия
чужого пользователя стала отвечать `200` с not-found-страницей внутри — мягкий 404 вместо
AR-2/NFR-005 AC-2. Поймано не рассуждением, а двумя красными тестами (`skeleton` — чужой
проект, `project-lifecycle` — удалённый). Лечение по документации Next: граница `<Suspense>`
**внутри** страницы, после проверки владельца; тело страницы вынесено в `SessionBody` без
изменения логики (D-167).

Остальное:

- **«Connection lost» — баннер, а не модалка эталона** (D-166). Д-1 сильнее визуального парити:
  диалог поверх сессии, у которой генерация продолжается на сервере, отнимает Stop ровно в тот
  момент, когда он нужен. Это **осознанное отличие своего оттенка**, и в чек-лист задачи 128 оно
  пойдёт именно так — прошу подтвердить на приёмке.
- Состояние баннера собрано **только из того, что уже узнают два существующих пути**:
  `onReachability` у session-request (ответ пришёл / не дошёл; дедлайн не отчитывается — «медленно»
  не значит «нет») и статус ридера (`reconnecting` и `failed:STREAM_DISCONNECTED` — потерян;
  `failed` от отказа провайдера — **не** потерян). Отдельного ping-эндпоинта нет: второе мнение о
  доступности — это вторая вещь, которая может ошибаться. Reconnect — это `router.refresh()`, а
  доказательством возврата служит сам пришедший рендер.
- **Названное ограничение** (D-165): в сценарии «неудавшийся session-request» баннер живёт
  мгновение — этот запрос всегда перечитывает страницу, и удачное перечитывание честно гасит
  баннер; а когда перечитывание тоже не проходит, Next уходит в полную навигацию и сбрасывает
  клиент. Поэтому живые проверки стоят на пути стрима, а состояние `checking` закрыто юнит-тестом.
  Если вы считаете, что баннер обязан переживать и первый сценарий, это меняет контракт
  перечитывания — скажите, и я вынесу отдельной задачей.
- Тосты: одна live-region в оболочке (`aria-live="polite"`), поднимается архивом/восстановлением,
  копированием и экспортом, каждый закрывается.

### 3. Задача 126 — completion-панель и честный handoff

- **Промпт описывает этот бандл, а не правдоподобный**: имена файлов — из плана методологии
  (OpenSpec-бандл получает `proposal.md`/`design.md`, тест на это есть), номера ревизий — те,
  которые отдаст экспорт, и едут вместе со строкой файла, а не доспрашиваются как «последняя»
  (после enrichment это разные ответы). Живёт в `specs/handoff/`, не в `prompts`: `prompts` —
  реестр того, что **мы отправляем модели**, а этот текст копирует пользователь (D-168).
- **Никакой имитации интеграций.** Кнопки Lovable/Bolt/Replit копируют промпт и открывают сайт;
  панель говорит это словами до нажатия («we do not send your bundle anywhere»). В e2e платформа
  застабана на сети — утверждение теста в том, что *мы* открываем этот хост, а не в том, что
  lovable.dev жив.
- **Download из панели побайтово равен экспорту** — потому что реализация одна: хелпер вынесен из
  панели экспорта, обе кнопки зовут его (D-169). Тест захватывает оба архива и сравнивает имена и
  содержимое файл за файлом.

### 4. Задача 127 — дифф и «go back»

- **Один рендерер диффа** вместо двух копий, токенами и с подложкой; каждая строка несёт
  `data-diff-line`, поэтому e2e спрашивает **вычисленный цвет** в обеих темах (D-170).
- **«Go back to previous step» — дописывание.** `POST /api/sessions/:id/revert` пишет Rev N+1 с
  содержимым Rev N-1 и штампом чата; триггеры неизменяемости (задача 16) не тронуты. Карточка
  сначала показывает дифф, применение — второе нажатие: откат был бы единственным действием
  продукта, меняющим содержимое без показанного диффа, — в задаче, которая как раз про Diff
  Preview. Слэш-команда `/go-back` жмёт ту же кнопку (механика задачи 121).
- **Почему новый эндпоинт, а не `proposed_changes`** (D-171): AC требует «with the edit chat as
  source», а путь одиночного предложения `source_session_id` при принятии не проставляет — его
  знает только batch, через `generation_runs`. Идти туда значило менять схему. Сессия в URL, а не
  файл, потому что источник ревизии — факт о том, кто сделал работу; переданный клиентом был бы
  утверждением. **Это добавление к API-поверхности — прошу подтвердить на приёмке.**
- Ссылка «Diff» в сайдбаре ведёт в URL-состояние вьюера (`?view=diff`) и появляется со второй
  ревизии.

### 5. Тесты и CI

| Что | Результат |
|---|---|
| `pnpm test:unit` | 93 файла, **1818** тестов — зелено |
| `pnpm test:boundaries` | 14 фикстур, включая три новых на цвет — зелено |
| `pnpm lint` / `pnpm typecheck` / `pnpm format:check` | зелено |
| `pnpm exec playwright test --project=chromium` | **80** тестов — зелено |
| Новые спеки на WebKit | `theme`, `connection` (дважды), `completion` — зелено |
| CI | #45 и #46 зелёные и смержены; #47 в прогоне; #48 открыт следом |

Новые e2e: `theme.spec.ts` (3), `connection.spec.ts` (4), `completion.spec.ts` (3),
`revert.spec.ts` (3). Новые юнит-наборы: `brand.test.ts` (58), `connection.test.ts`,
`toast.test.ts`, `handoff-prompt.test.ts` (6), `revert.test.ts` (5), плюс четыре кейса
достоверности в `session-request.test.ts`.

### 6. DECISIONS

D-162…D-164 (задача 124), D-165…D-167 (задача 125), D-168…D-169 (задача 126),
D-170…D-171 (задача 127).

### 7. Что нужно от Архитектора

1. **Баннер вместо модалки** (D-166) — подтвердить как осознанное отличие своего оттенка для
   чек-листа задачи 128, либо сказать, что нужна модалка, и тогда мне нужен ваш вердикт по Д-1.
2. **Новый эндпоинт `POST /api/sessions/:id/revert`** (D-171) — добавление к API-поверхности
   solution.md. Подтвердить или предложить другой путь (альтернатива — колонка `source_session_id`
   на `proposed_changes`, то есть миграция).
3. **Ограничение баннера в сценарии session-request** (D-165) — принять как названное свойство
   или завести отдельную задачу.

### 8. Следующий шаг

По вашему порядку остаются **131 (А-10), 128 (парити-чек-лист + ultracode red-team), 129 (гейт)**.
Гейтовую квоту Google в этом раунде не тратил ни на один вызов — все прогоны на стабе.
