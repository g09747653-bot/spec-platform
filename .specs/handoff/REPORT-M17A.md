# REPORT M17а — Telegram и голос

## Раунд 1 — 2026-08-21 · режим MAX · ОСТАНОВКА ПО ПРАВИЛУ ЗОНДА 165 (А-28 п.2): подписочный путь аудио не принимает; выбор звена транскрибации — решение заказчика

### 1. Итог одной строкой

Пройдено по порядку А-27: тег `m16a-accepted` → мелочь §7.5 → 175 (адаптер штатно, живая проба GREEN) → 176 → 164 (бот целиком, 11 интеграционных кейсов) — а зонд задачи 165 дал предусмотренный директивой отрицательный исход: **CLI/адаптер подписки аудио не обрабатывает** (три именованных отказа), тогда как **google-звено транскрибировало тот же .ogg с отличным качеством** — и по А-28 п.2 это честная остановка с изложением вариантов, решение заказчика (§4).

### 2. Сделано и принято к сдаче

- **Тег** `m16a-accepted` → dbd6a31 (первое действие сессии), запушен.
- **Мелочь §7.5**: [e2e/gate-M14a.live.ts](../../e2e/gate-M14a.live.ts) — при `GATE_RESTART=0` прогулка больше не пишет PROBLEM «рестарт не исполнен»; вместо лжи ярлыком — строка «рестарт выключен, это заказ».
- **Задача 175 — адаптер подписки штатно.** Мост из scratchpad стал модулем `loop/src/bridge/` (prompt/cli/server/serve), поднимается `pnpm bridge`; три урока D-304 закреплены юнитами (система — только `--append-system-prompt`; промпт — stdin, в argv его нет по построению; `--tools ''` + изоляция). Контур получил **собственное звено `claude-cli`** (`CLAUDE_CLI_API_BASE`; журнал, называя провайдера, называет бюджет — D-307); платформа не менялась ни строкой (подключение прежним `OLLAMA_BASE_URL`). **Живая проба**: локальный профиль платформы, цепочка «адаптер спереди, google запасным» — автономный драйвер за 8 шагов довёл до принятой конституции **6479 знаков, 9 секций**; вызов генерации через мост 83,5 с; интервью (2 раунда) тем же звеном; **утечек токена в артефактах — ноль** (grep по всем файлам и dev-server.log). Артефакты: `artifacts/probe-175/`.
- **Задача 176 — причина красноты исполнителю.** Вердикт приёмки — файлом `handoff/reports/verdict_<taskId>.md`: пишется при FAILED (включая таймаут) и RETURNED, стирается при COMPLETED; промпт исполнителя велит читать его первым. **«SUCCESS без единой правки при красной причине» — именованный отказ ДО приёмки** (mtime-обход с ранним выходом, минуя `handoff/`; допуск часов 2 с в безопасную сторону — D-308). Инструкция архитектора заданий получила строку «команды тестов — POSIX sh, приёмка гоняет `sh -lc`», закреплена фикстурным тестом. Стаб-циклы: красный→вердикт на диске; повтор без правок→именованный отказ, приёмка не запускалась (0 gate-контейнеров); повтор с правками→приёмка своим чередом; зелёный→вердикт унесён.
- **Задача 164 — Telegram-бот.** `loop/src/gateway/`: клиент Bot API на long polling (значение токена не покидает модуль; ошибки вымарываются до выхода — включая описания самого Bot API); **закрытый бот** — чужие сообщения молча, включая `/start` (в тесте незнакомцу не уходит ни байта); меню `setMyCommands`, русский онбординг `/start`, `/status` по доске (вехи/задачи/живой конвейер/причина заморозки), `/stop` с подтверждением кнопкой → `freezePipeline` («остановлено владельцем из Telegram»); задумка → кнопки «🚀 Запустить / ✖️ Отмена» до каких-либо действий (дух P2); **алерты по каждому событию шины**: запуск (новая публикация в start-loop — событие, которого у строки БД не было), красный CI с причиной из FROZEN.md, возобновление (различается с запуском по прежнему статусу), успех, блокировка с текстом `BLOCKED_*.md`. Швы `launch`/`transcribe` инъецируемые — их подключают 166/165; неподключённые отвечают именованно. Без пары токен+chat id контур работает как вчера, консоль называет недостающую переменную. Интеграционный тест с **фейковым Bot API** (настоящий HTTP, offset-семантика long poll): 11 кейсов, включая красное условие токена.
- **Бухгалтерия**: MaxListeners — строкой в Backlog (А-27 п.4); улика `.local/db-m15a-stalled-040556` удалена (А-27 п.5); `chat_id.txt` → `loop/.env` `TELEGRAM_OWNER_CHAT_ID`, файл удалён; `env.loop.txt` (токен бота передан заказчиком) → `loop/.env` `TELEGRAM_BOT_TOKEN`, файл удалён — значения нигде не печатались; D-307/D-308/D-309.
- **Тесты**: loop 285/285 локально (25 файлов); линт/типы чисто. PR **#68** (мелочь+175+176+164): Lint/boundaries/types/unit — GREEN, E2E-матрица на момент рапорта дозревает; вольюсь по зелёному.

### 3. Зонд 165 — методика и дословные исходы (артефакты `artifacts/probe-165/`)

Фикстура: русская речь «Собери консольный планировщик личных дел…» синтезирована локально (SAPI Microsoft Irina ru-RU) → `idea-ru.wav`, конвертирована ffmpeg в **`idea-ru.ogg` (opus 32k, формат голосовых Telegram)** и `idea-ru.mp3`.

Подписочный путь (директива: «аудио через адаптер 175/CLI») — **три захода, три именованных отказа**:

1. **CLI + инструмент Read, .ogg** (`probe-a-read-ogg.json`): «Я не могу транскрибировать аудиофайлы — инструмент чтения поддерживает только текст, изображения и PDF, но не аудио (.ogg). У меня нет доступа к средству распознавания речи» — и агент сам предлагает whisper через Bash, то есть НЕ подписочное звено.
2. **CLI + @-вложение файла в промпт, .ogg** (`probe-b-mention-ogg.json`): «нет возможности обрабатывать аудио… работаю только с текстом и кодом».
3. **CLI + Read, .wav** (`probe-c-read-wav.json`): «поддерживает текст, изображения, PDF и Jupyter-ноутбуки, но не аудио».

Флаг `--file` из `--help` — «File resources to download at startup», загрузка ресурсов, не аудио-вход модели. Мост (адаптер 175) аудио-части OpenAI-формата не переносит по построению (текстовые content-part'ы). Вывод зонда: **у подписочного пути нет модальности аудио** — отказ не формата, а тракта; конвертация ffmpeg здесь ничего не лечит. Замечание к директиве заказчика («claude неплохо расшифровывает голос»): это опыт приложения Claude, где голос распознаёт само приложение до модели; на API/CLI-тракте такого звена нет.

**Запасной замер для вариантов** — google-звено, тот же `.ogg` без всякой конвертации (`probe-d-google-ogg.json`): `gemini-3.5-flash` (модель контура из loop/.env, существующий ключ), inline-аудио → **200, транскрипция дословная**: «Собери консольный планировщик личных дел. Нужно добавлять дела с датой, показывать список на сегодня и отмечать сделанные. Данные храни в локальном файле.» Расхождения с оригиналом — только число («дело→дела», «сделанное→сделанные») на синтетическом голосе; смысл 1:1.

### 4. Варианты (изложение по А-28 п.2 — решение заказчика)

| Вариант | Что нужно | Качество (замер) | Риски/цена |
|---|---|---|---|
| **(а) google-звено умолчанием** (`gemini-3.5-flash`, существующий ключ) | только конфиг + хук `transcribe` в готовый шов шлюза; ffmpeg, вероятно, не нужен вовсе — ogg/opus принят напрямую | отличное на русской речи (§3) | free tier: квоты и уже пережитый abuse-блок (гейт M16а); голос владельца уходит в облако Google |
| **(б) Whisper локально** (`WHISPER_API_BASE` — опция уже в конфиге) | операционный шаг заказчика: поставить whisper-сервер (faster-whisper / whisper.cpp); тот же хук `transcribe` | высокое ожидаемо (whisper на русском силён), не замерено — нечем без установки | установка и VRAM/CPU на машине; зато весь голос локален |
| **(в) отложить голос** | ничего | — | текстовый путь равноправен и уже работает (164); голосовая задумка живьём — финальная приёмка, её пришлось бы переопределить |

Рекомендация исполнителя (решение — ваше): **(а) как немедленное умолчание** — единственный вариант без новых установок, замерен на настоящем формате Telegram; **(б) заявить постоянной целью** после живой приёмки, если политика «голос не покидает машину» важна. При выборе (а) достройка 165 — малая: хук `transcribe` (скачивание файла ботом уже есть в клиенте: `getFilePath`/`downloadFile`), кнопочное подтверждение уже построено и оттестировано в 164, зонд формата пройден.

### 5. Состояние и что дальше

- Ветка `m17a-telegram-voice`, PR #68; после зелёной E2E-матрицы волью (постоянное разрешение). Секция рапорта закоммичена туда же.
- 166 (фасад) и 167 (гейт) не начаты: 166 зависит от 165 по плану, у гейта фикстурная транскрибация — «тем путём, который выбрал зонд», а путь не выбран.
- По вердикту достройка 165(а) + 166 + гейт 167 помещаются в одну следующую сессию.

### 6. Вопрос Архитектору

Один: **какое звено назначается умолчанием транскрибации — (а), (б) или (в) из §4?** (При (а): подтвердите, что голос владельца через google-облако приемлем; при (б): операционный шаг за вами, назовите порт — `WHISPER_API_BASE` уже ждёт в конфиге.)
