# Карта режимов отказа контура (Программа А)

> Пишет Архитектор. Ответ на замечание заказчика 2026-08-24: «в чём проблема изучить как исследователь, рассудить как учёный и только потом формулировать дорогу». Замечание принято: до сих пор мы чинили то, что сломалось, а не то, что может сломаться. Ниже — разбор по стадиям: чем каждая может выдать **неверный результат, выглядящий верным**. Это не список задач; это карта, по которой ставятся задачи.
>
> Столбец «состояние»: **вылечено** — есть код и **названная живая регрессия**; **правится** — назначено, не доказано; **ОТКРЫТО** — не покрыто ничем; **не проверено** — механизм есть, отказ не воспроизводился.
>
> **Правило А-42 о самой карте:** статус «вылечено» ставится ТОЛЬКО при названной живой регрессии. Нет регрессии — «правится», даже если код написан и выглядит верным. Правило родилось из строки «Троттлинг подписки — вылечено», простоявшей неверной семь недель: код был, регрессии не было, и лечение само оказалось дефектом. **Ревизия по этому условию проведена 2026-08-24 (А-42 п.1): понижено пять строк**, каждая помечена «Понижено ревизией А-42» с причиной.

## 1. Приём задумки (TG → SEED)

| режим отказа | наблюдался | состояние |
|---|---|---|
| Задумка искажена при переносе в SEED | да (сужение брифов) | вылечено (запрет сужения, SEED дословно) |
| Неявная мера не извлечена: «копия» — это сколько процентов? | да (весь бенчмарк-цикл) | **правится** (А-41: мера по умолчанию, вопрос при сомнении) |
| Двусмысленность СУТИ не распознана — контур гадает вместо вопроса | да | **правится** (А-41: один уточняющий вопрос ДО сборки) |
| Невыполнимая задумка принята как выполнимая | нет | **правится** (А-42 п.2: суждение о выполнимости на интейке — перепись материала кодом, перечни моделью, вердикт кодом, условия в план, алерт ДО сборки). Живой регрессии на прогоне ещё нет — статус по правилу А-42 |
| **Планку в SEED поставили МЫ и приписали её человеку** | да (А-43: «пиксель в пиксель» в трёх SEED подряд — операторский текст; заказчик этих слов не произносил ни разу) | **ОТКРЫТО** — правило есть, механизма нет: **у всякой планки в SEED называется источник; планка, которой нет в словах человека, в SEED не пишется.** Это тот же приём, что дал А-39 («расхождения объявляй заранее»), только источник искажения — мы сами, и потому его труднее заметить: SEED выглядит дословной задумкой владельца и читается всеми как она |

## 2. Спецификация (платформа → бандл)

| режим отказа | наблюдался | состояние |
|---|---|---|
| ТЗ полное и грамотное, но не про результат | да (исходный провал) | вылечено (вершинный критерий, суд полноты) |
| Критерии приёмки механические, не результатные | да | вылечено (суд трёх осей поверх задач) |
| Критерий недостижим по построению | да (WA04, ворота ≤1%) | вылечено (D-323) |
| Бандл валиден по схеме, но пуст по смыслу | да (выжимки) | вылечено (смок обеих выжимок) |

## 3. Суд полноты плана

| режим отказа | наблюдался | состояние |
|---|---|---|
| Судья пропускает реальный пробел | да (раунд А-37.1, ход 3) | **не проверено** — пропуск случился, регрессии на него нет |
| Судья придирается бесконечно (петля) | да (задача 170) | вылечено (единственный переспрос → скелет) |
| Пробел не возвращается автору плана | да | вылечено (D-321) |

## 4. Классификация артефакта

| режим отказа | наблюдался | состояние |
|---|---|---|
| Неверный класс → неверная стратегия дробления | да (**корень исходного провала**) | вылечено (artifact-class, потолок 8, владелец целого) |
| Смешанный проект: части делятся, а дизайн-система общая | нет | **ОТКРЫТО** — классификатор бинарный, третьего случая нет |
| Класс верен, но потолок долей неверен для размера | нет | **ОТКРЫТО** — потолок 8 назначен, не выведен |

## 5. Планирование и нарезка

| режим отказа | наблюдался | состояние |
|---|---|---|
| Дробление разрушает связность по построению | да (**корень**) | вылечено (цельно-артефактный план) |
| Никто не владеет целым | да | вылечено (одна задача владеет) |
| Тупик или цикл в графе зависимостей | нет | **не проверено** |
| План на сотни задач: очередь, память, стоимость | нет | **ОТКРЫТО** — проверялись 6 и 46, не 300 |
| **Планирование последовательно и стало потолком прогона** | да (А-44 п.5: интейк 127,4 с против 29,0 с стены конвейера; десять независимых заданий пишутся десятью `await` подряд) | **вылечено** (А-51 п.3: веер с потолком `LOOP_INTAKE_CONCURRENCY` на стадии заданий — единственной, где вызовы независимы; поднято А-52 по живому прогону и регрессиям без Docker). Регрессии в общем прогоне: `fan-out.test.ts` («РЕГРЕССИЯ: одновременно в полёте не больше потолка», «порядок результатов — порядок входа», «отказ одной работы — отказ веера») и `intake.test.ts` («РЕГРЕССИЯ (А-51 п.3): задания пишутся ПАРАЛЛЕЛЬНО, а порядок плана и ленты — прежний»). Живой прогон А-52 п.2 (бандл M14а, мост подписки): пять одновременных запросов заданий в журнале моста одной секундой; интейк 636,4 с → 282,2 с, **ускорение 2,25×** (симуляция обещала 2,50×), стадия заданий 2,49×. Таблица и расхождение числом — INTAKE-STAGES.md |
| **Стену интейка не разложить по стадиям — оптимизировать нечем, кроме догадки** | да (А-51 п.3: «127,4 с» было одним числом за шестью работами) | **вылечено** (А-51: спаны стадий в `IntakeResult.stages`, строка разбивки в ленте каждого интейка, таблица в отчёте замера параллельности, отдельный замер `intake-stages-cli`) |
| **Ширину вехи задаёт БАНДЛ, а не контур — и это нигде не было записано** | да (разведка А-51 п.4) | **ОТКРЫТО как ограничение (контур задач не порождает — это устройство), но нотация ВЫЛЕЧЕНА в платформе (А-52)**: планировщика, который сам порождает задачи для системного класса, у контура НЕТ — общая ветка режет `bundle.tasks`, а бандл пишет платформа. Ширина вехи = ширина слоя Kahn (если бандл заявил зависимости) либо размер фазовой группы (если нет). Замер `plan-width-cli`: M16а — вехи **10/10/10/2/1**, потолок **6,60× из 10**; M14а живой (плоско, ноль зависимостей) — **2,67× из 10**. **А-52 закрыл нотацию с обеих сторон**: генератор планов платформы требует явных `dependsOn` у каждой задачи ОБОИМИ путями (парити task 169; методология — А-52, блок зависимостей в промпте), машинный экспорт даёт named-предупреждение `flat-plan` в выжимке при плоском плане больше 8 задач. Регрессии без Docker: канонический близнец слепка M14а → нарезка **dependencies**, **3,20×** и пик 5 против 2,67× и пика 4 у слепка (`plan-width.test.ts`, «нотация решает ширину»); та же запись с вычеркнутыми зависимостями → вырождение до 1,00× — то, что предупреждение и ловит |
| **Веер дорожает по ходу и роняет долю разобранных ответов** | да, один прогон (А-52 п.2: средний веерный ход моста 60,4 с против 48,2 с последовательного, хвост 147,8 с; деградаций заданий 4 из 13 при веере против 0 из 12 последовательно) | **не проверено — симптом, причина не найдена** (правило §10): подписочный CLI, похоже, делит канал между одновременными ходами, а деградации при веере могут быть его же ухудшенными ответами — но один прогон не отличает механизм от совпадения. Гарантии целы: деградация — заготовка со строкой WARN, не дыра в плане; ускорение веера всё равно 2,25×. Мерить на втором живом прогоне, прежде чем чинить |
| **Живое суждение об объёме режет план недетерминированно** | да (А-52 п.2: тот же бандл M14а, бюджет 44 — потолок 1 срезал 16→12, потолок 5 срезал 16→13; в симуляции с ценами по 1 стадия была формальностью) | **не проверено — поведение НОВОЕ и названо, вердикта нет**: сокращение работает (А-51 п.3 живьём, до сборки, с алертом), но разброс сокращений между прогонами — свойство модельной оценки цены, и его цена владельцу не судима одним прогоном. Петля калибровки §10.4 (предсказание против факта суда) — штатный механизм, который это выправит; до живых прогонов суда коэффициент не двигается |
| **Объём против бюджета не судит никто** | да (замер заказчика: NEURA 44 ссылки и ноль мёртвых, копия 86 и 74 мёртвых) | **вылечено** (А-44 п.4 + А-51 п.3; поднято А-52 по живым регрессиям без Docker, все в общем прогоне): `scope.test.ts` «РЕГРЕССИЯ: резка на РЕАЛЬНОМ превышении, а не на „всё влезает"», «„один пункт дороже бюджета" — свой вердикт и свой алерт», «промпт НЕ называет модели бюджет»; `intake.test.ts` «условия объёма и выполнимости доезжают до задания ОБЩЕЙ ветки», «сокращённой задаче задание не пишется вовсе», «сокращение отменяется для задачи, от которой ЗАВИСИТ удержанная». Живой интейк А-52 п.2 прошёл стадию объёма на общей ветке живьём (бандл M14а, системный класс) |

## 6. Вехи, очередь, состояние

| режим отказа | наблюдался | состояние |
|---|---|---|
| Ключи вех не разведены по проектам | да (D-324) | вылечено |
| Ключи задач не разведены; чужие статусы наследуются | да (D-325 — **лживая галочка из индекса**) | **вылечено** (А-38 п.3: составной ключ `(project_id, task_id)`/`(project_id, milestone_id)`, миграция 0003, регрессия на двух проектах с одинаковыми `BT01`) |
| Возобновление роняет цельный план в нарезку | да (D-326) | вылечено |
| Конвейер не переходит между вехами сам | да, 4 раза | **вылечено** (А-38 п.1) — и диагноз был не тот: планировщик переходы умел, старты были заперты ложным тарифом (строка ниже) |
| **Предупреждение тарифа прочитано как отказ** | да (А-37.1, четыре веха подряд) | **вылечено** (А-38 п.1): `allowed_warning` — разрешённый вызов; читался как отказ и запирал старты на 216 162 с. Регрессия несёт дословную полезную нагрузку живого прогона |
| **Проход стоит молча: ни возврата, ни строки в ленте** | да (А-37.1) | **правится** (А-38 п.1): уход при непустой очереди называет причину, а «готовых идти» без причины объявляется дефектом управляющего потока — но КОД БЕЗ РЕГРЕССИИ: ни один кейс не утверждает названную строку. Понижено ревизией А-42 |
| **Два конвейера над одной доской** | да (А-37.1: четыре нажатия = четыре прохода) | **вылечено** (А-38 п.1): один водитель на проект, второй запуск — именованный отказ |
| Доска расходится с диском | да (D-325) | **правится** (инвариант 1 стенда спрашивает на каждом переходе; регрессии на дрейф живого индекса нет) |
| Возобновление кнопкой, рассчитанной на красные | да (409) | **ОТКРЫТО** — назван, не чинился |

## 7. Раздача и исполнение

| режим отказа | наблюдался | состояние |
|---|---|---|
| Изоляция контекстов рассогласовывает результат | да (**корень**) | **правится**: для цельных артефактов лечение есть и с регрессией (форма плана, один владелец целого); для систем заявлено «по построению» — это довод, а не живая регрессия. Понижено ревизией А-42 |
| Модель молча уходит от брифа | да («Neuro») | частично (спрашивает до прогона; гарантии нет) |
| Потолок итерации убивает честную работу | да (300с) | вылечено (900с + per-task) |
| Троттлинг подписки | да | **правится**: пауза стартов работает, но САМА ОКАЗАЛАСЬ дефектом (А-38 п.1) — лечение читало предупреждение как отказ. Строка «вылечено» здесь была неверна семь недель |
| Материал есть, но не применён | подозревалось, **опровергнуто** (135 из 141 применены) | — |
| Материал неполон, и об этом молчат | да (шрифт, половина картинок) | **правится** (А-39: объявлять заранее) |
| Исполнитель выдал демо вместо продукта | да (**исходный провал**) | вылечено (суд трёх осей) |

## 8. Контролёр и приёмка

| режим отказа | наблюдался | состояние |
|---|---|---|
| Линтеры зелёные при сломанном продукте | да (**исходный провал**) | вылечено (суд поверх) |
| «Принято» без контейнера | да (D-325) | правится (инвариант) |
| Глаза суда вне контейнера (хостовая слепота) | да (D-314) | вылечено |
| **Приёмка polish/measure штампует заявление исполнителя** | да (прогон А-43, WA04/WA05/WA06) | **вылечено** (А-44 п.1 + А-51 п.1; поднято А-52 по живой регрессии). Замер прогоняет приёмка своим образом, чужой отчёт сносится первым действием, `\|\| true` исчез, книга сходимости — снаружи рабочей директории. Живая регрессия БЕЗ Docker: `gate/measurement.test.ts` «шелл замера исполняется по-настоящему» — настоящий POSIX-шелл, настоящая временная директория, и «РЕГРЕССИЯ: отчёт, подложенный ИМЕННО в recordPath, шелла не переживает» (плюс: свой отчёт проходит и число читается из него; не прогнавшаяся команда краснеет кодом 64; встроенный `exit` остаётся красным). Шелла нет — кейс ПАДАЕТ, а не пропускается. Прежний разбор: — четыре улики: (1) замер в приёмочном контейнере не идёт и НЕ МОЖЕТ (образ без браузера: `browserType.launch: Executable doesn't exist` — трижды за прогон); (2) провал глотает `\|\| true`, поставленный по D-323 против ПОРОГА, но глотающий и «кадров нет»; (3) `test -s report.json` проходит на файле, который написал сам исполнитель (`generatedAt` внутри его итерации); (4) память сходимости `.loop-convergence.json` пишет ТОЖЕ исполнитель — он гоняет ту же команду внутри своего контейнера, где браузер есть, — поэтому приёмка сравнивает число исполнителя с его же записанным числом: на WA06 это дало «52.5925435555148 (было 52.5925435555148)», сравнение значения с самим собой. Числа при этом настоящие; ложна независимость приёмки |
| Приёмка падает на устройстве среды (симлинки) | да (cpSync) | **правится**: копия делается контейнером, и это верно, но регрессии на симлинк нет ни одной — класс доказан живым гейтом, не кейсом. Понижено ревизией А-42 |
| **Книга сходимости лежит под пером того, кого по ней судят** | да (проверка А-51: `executor/run.ts` монтирует рабочую директорию НА ЗАПИСЬ, а книга лежала в `handoff/MEASUREMENTS.json` внутри неё) | **вылечено** (А-51 п.1: книга уехала в `<корень>/.loop-ledger/<проект>.json` — на уровень выше любого исполнительского бинда; нечитаемая книга КРАСНИТ задачу, а не отдаёт пустую; запись поверх испорченной книги запрещена, чтобы порча не стирала базовую линию. Живые регрессии: книга снаружи, порча краснеет, запись отказывает — все без Docker) |
| **Ключ сравнения назначает тот, кого судят** | да (находка разведки А-51) | **вылечено** (А-51 п.1: перенос книги закрывал подделку ЧИСЛА и не закрывал подделку КЛЮЧА — `recordPath#divergenceKey` берётся из задания, а задание перечитывалось ПОСЛЕ итерации из того же rw-монтирования. Достаточно было поправить у себя `recordPath`, и прошлого числа по новому ключу нет: сходимость проходит «первым замером цепочки». Теперь блок замера берётся из чтения ДО итерации, а расхождение называется в ленте ошибкой) |
| **Сходимость блокирует полировку бесконечно** | нет (найдено рассуждением, вердикт §10.2) | **вылечено** (А-51: предохранитель `CONVERGENCE_PATIENCE = 3` — после трёх заходов подряд без улучшения задача ПРИНИМАЕТСЯ с записью «сходимость достигнута исчерпанием». Порогом это не становится: сравнение по-прежнему со своим прошлым, а не с константой) |
| **Встроенный `exit` в команде замера минует метку отказа** | да (найдено ИСПОЛНЕНИЕМ настоящего шелла, А-51) | **не проверено на живом прогоне, но закреплено кейсом**: `{ …; }` есть группировка, а не подоболочка, и `exit 7` внутри неё завершает весь шелл, не дав сработать ветке `||`. Метка `MEASUREMENT_FAILED` тогда не печатается. Чинить подоболочкой не стали: `( … )` не сохранила бы `cd` команды плана, а `recordPath` читается после неё тем же шеллом. **Гарантия цела: исход всё равно красный, причина названа кодом возврата.** Кейс стоит именно на этом |

## 9. Суд качества

| режим отказа | наблюдался | состояние |
|---|---|---|
| Звено судьи не видит изображений | да | вылечено (цепочка отбирает зрячее звено) |
| Судья подыгрывает своему конвейеру | проверено — **не подыграл** | **правится**: один живой прогон (А-37.1) — не регрессия; строка ниже про «зелёный при плохом продукте» говорит это же о соседнем режиме. Понижено ревизией А-42 |
| Судья зелёный при плохом продукте | проверено — не случилось | **не проверено** системно (один прогон — не доказательство) |
| **Судья не пользуется продуктом: три оси судят ПОКОЙ** | да (А-44: 74 из 86 ссылок в никуда, тост «Демо-версия, функция недоступна», ломающееся мега-меню — ни одного суд не увидел) | **правится** (А-44 п.2: четвёртая ось «работоспособность», проба открывает продукт браузером, наводит и кликает; три исхода различаются; живой кейс `quality-stage.integration.test.ts` ловит все три дефекта) |
| **Суд в проде — мёртвый код** | да (`visual-judge.ts` импортировался только своим тестом; суд все раунды выносил операторский скрипт) | **правится** (А-44 п.2: суд стал стадией конвейера, вердикт входит в вершинную строку дословно) |
| **Проба принимает якорь за навигацию, прокрутку — за раскрытие** | да (первый и второй живые прогоны самой пробы) | **вылечено** (А-44 п.2: сравнение URL без якоря, координаты документа вместо оконных; живая регрессия — фикстура с шестью мёртвыми ссылками) |
| Судья невизуального артефакта | — | **ОТКРЫТО, но цена перестала быть тихой** (А-51, вердикт §10.1): «не судимо судом» стало ЯВНЫМ разрешённым исходом по образцу «не проверяемо приёмкой». Признак ставит КОД пробы изнутри контейнера (нет ни одной страницы — открывать нечего), модель объявить свой продукт несудимым не вправе. Проект закрывается, долг называется вслух и считается; «продукт хорош» отсюда не следует и сказано это дословно. Сама строка карты остаётся ОТКРЫТОЙ: судьи для невизуального у контура по-прежнему нет. **Счётчик долгов суда (публикуется с раунда А-52): 0** — исход существует с А-51, живых прогонов суда качества с тех пор не было (интейк-прогон А-52 до суда не доходит по построению); каждый следующий раунд с живым прогоном публикует счётчик заново |
| Судья для НЕВИЗУАЛЬНОГО артефакта | — | **ОТКРЫТО — главное ограничение масштаба** |
| **Без провайдера судьи зелёной доски не получает НИ ОДИН проект** | да (находка III проверки А-44; живой производственный дефект) | **вылечено** (А-51 п.4: ось связности требовала `chain !== null`, а прод отдаёт `null` при пустом перечне провайдеров роли `judge`. Теперь несудившаяся ось — ДОЛГ, а не красное: доска зелена по судимым осям, долги названы поимённо и посчитаны, «зелено по всем четырём осям» при долге не произносится) |
| **Вершинная строка называет несостоявшийся суд красным** | да (находка V проверки А-44) | **вылечено** (А-51 п.4: исходов четыре — зелено, красная ось, суд не состоялся, продукт не судим, — и вершинная строка называет случившийся. Прежде «но суд качества красный» зашивалось в обе незелёные ветки, а состояние проекта, поле `green` и заголовок алерта были у них тождественны) |
| **Число инертных занижено дедупликацией переписи** | да (находка IX проверки А-44; «74 из 86» превращались в «4») | **вылечено** (А-51 п.5: перепись несёт `duplicates` — сколько одинаковых элементов представляет улика, — и доска называет ОБА числа разными словами: «нажато» о работе пробы, «элементов» о продукте. Потолок пробы объявляется по факту, а не по неравенству чисел: при чистом обрезании без дублей `total` совпадал с числом улик, и потолок молчал вовсе) |
| **Перечень самообъявлений ловит пометки разработчика** | да (находка VII: `placeholder`, `lorem ipsum`) | **вылечено** (А-51 п.5: перечень сужен до фраз, ОБРАЩЁННЫХ К ПОЛЬЗОВАТЕЛЮ; изъяты `placeholder` (есть у любой честной формы), `lorem ipsum` (законна на макете) и `todo:` (комментарий, которого пользователь не видит). Регрессия: `<input placeholder="Ваше имя">` не краснит ось) |
| **Живой якорь объявлен инертным** | да (находка VIII: `href="#news"` и `href="#"` давали один исход) | **вылечено** (А-51 п.5: проба собирает `id`/`name` страницы и отдаёт улику `anchorResolves`; решает по ней КОД. Прокрутка признаком быть не могла — клик по `href="#"` в Chromium уводит документ в начало, то есть мёртвая ссылка «двигала» бы страницу, а `href="#нет-такого"` не двигал бы ничего) |
| Живость: анимация написана и не работает | да | вылечено (поведенческие пробы) |

## 10. Отчёт и доставка

| режим отказа | наблюдался | состояние |
|---|---|---|
| Лживая галочка: «готово» на невыполненной задумке | да (**причина отклонения приёмки**) | **вылечено и доказано** (А-37.1) |
| Расхождения объявлены после показа, а не до | да | правится (А-39) |
| **Итог прогона уходит в тишину, когда он не «завершён»** | да (А-44: суд стал стадией, `project-status` при незелёной доске не публикуется — «все задачи приняты, продукт не принят» владельцу не приходило) | **вылечено** (D-338: событие шины `quality`, вердикт уходит владельцу дословно; две регрессии в `gateway.test.ts`). Тот же класс, что «проход стоит молча», только с другой стороны |
| **Реестр расхождений в одну колонку легализует недоделанное** | да (А-44: «расхождений N» читается как «мы не могли», хотя означает «мы не стали») | **вылечено** (А-44 п.3 + А-51 п.2: числа раздельны, и род записи стал ПОЛЕМ `kind` машинного `DEVIATIONS.json`, а не фразой в заголовке markdown) |
| **Различение родов — текстовая эвристика над markdown, и она молча выдаёт сокращения за замены** | да (установлено ИСПОЛНЕНИЕМ модуля, проверка А-51 XI: плоский реестр с титулом `# Реестр расхождений: замены по материалу и сокращения объёма` давал `{material: 2, scope: 0}` и печатал владельцу «замен по материалу 2, сокращений объёма 0» — ни слова о старой форме) | **вылечено** (А-51 п.2: род — поле закрытого перечня в `DEVIATIONS.json`, проверяемое схемой; реестр без поля объявляется НЕРАСПОЗНАННОЙ ФОРМОЙ и числа по родам по нему не называются вовсе — приблизительный род читается как точный, и это хуже отсутствующего. Побочно вылечены три ошибки того же механизма: подзаголовок внутри раздела, перифраз «Замена» вместо «Замены», строка `\| - \| - \|` как разделитель) |
| Продукт разбросан, входа нет | да | вылечено (А-35.2, один вход) |
| Отчёт честен, но человек его не понимает | нет | **ОТКРЫТО** — читаемость отчёта не судится ничем |
| **Находка названа симптомом, а не механизмом — и принимается как диагноз** | да (А-37.1, находка 4: «не переходит между вехами» вместо «ложный тариф запер старты») | **ОТКРЫТО** — правило: находку в рапорте называть механизмом; там, где механизм не вскрыт, писать «симптом, причина не найдена». Отчёт был честен и всё же увёл работу не туда |

## 10б. Стенд контура (появился в А-38)

| режим отказа | наблюдался | состояние |
|---|---|---|
| Заглушка расходится с живым исполнителем — стенд зелен при сломанной работе | нет | **правится**: обязательная живая смок-задача в каждом прогоне (`pnpm bench`, код возврата ≠ 0 без неё); заглушка печатает дословную строку живого CLI |
| **Инварианты состояния не видят НЕПОДВИЖНОСТИ** | да (А-38: ни один из пяти не сработал на «без нажатий») | **правится**: стенд отдельно меряет живость прохода и на сломанном тарифе краснеет на всех пяти рубежах — но замер сделан руками, автоматического кейса на срабатывание сторожа нет. Понижено ревизией А-42 |
| Инварианты костенеют и орут зря при законной смене дизайна | нет | **ОТКРЫТО** — назван в А-40 п.5д, не наблюдался |
| Стенд разрастается в фреймворк тестирования | нет | **ОТКРЫТО** — удерживается только правилом «растёт — остановись и доложи» |
| **Заглушка с пустым перечнем провайдеров делает стадию бесплатной и сдвигает все ответы** | да (А-51, при первом же прогоне замера стадий) | **вылечено кейсом замера**: исследователь спрашивает модель лишь при непустом `chain.providers` (`researcher.ts`), и стенд с `providers: []` показал «исследование — 0 вызовов, 0,0 с», а сценарные ответы уехали на один вопрос вперёд — класс получил прозу исследователя, выполнимость получила ответ класса. **Стенд, у которого звено отвечает, но не числится, меряет не тот конвейер.** Замер стадий теперь даёт звену фиктивного провайдера |

## 11. Продолжение разработки (второй этап)

| режим отказа | наблюдался | состояние |
|---|---|---|
| Контур рассчитан на один проход насквозь | да — **D-324/D-325/D-326 суть его симптомы** | **ОТКРЫТО ЦЕЛИКОМ** |
| Нельзя указать существующий проект и продолжить | — | **ОТКРЫТО** |
| Нельзя дополнить план новой задумкой поверх готового | — | **ОТКРЫТО** |
| Нет памяти решений между заходами | — | **ОТКРЫТО** |

## Сводка: где мы слепы

Вылечено и доказано — то, что нас било: лживая галочка, дробление целого, механическая приёмка, слепой судья, коллизии индекса, ложный тариф и молчаливое стояние прохода.

**Открыто и не било ещё ни разу — то, на чём мы встанем дальше:**

1. **Судья для невизуального** — потолок для игр и сервисов. Без него масштаб закрыт.
2. **Второй этап целиком** — продолжение работы над готовым проектом. Три уже найденных дефекта индекса были его ранними симптомами.
3. **Масштаб плана** — 300 задач против проверенных 46 (стенд прошёл 50 заглушкой; это про трубу, не про модельную стоимость).
4. **Смешанный класс** — части делятся, дизайн-система общая.
5. **Суждение о выполнимости на входе** — контур не умеет сказать «этого нельзя» до того, как возьмётся.
6. **Читаемость отчёта человеком** — судится честность, не понятность.

Первые два — вехи, а не правки. Остальные — задачи.

## Урок метода из А-38 (дописан после сессии)

Карта составлялась по вопросу «чем стадия может выдать неверный результат, выглядящий верным». Сессия А-38 показала **седьмой класс слепоты, которого в этой постановке не было: неподвижность**. Все одиннадцать разделов спрашивают о СОСТОЯНИИ — правильно ли записано, сходится ли с диском, не перехвачено ли чужим. Ни один не спрашивал, ДВИЖЕТСЯ ЛИ система: конвейер, стоящий на ложном тарифе, не нарушал ни одного инварианта состояния — доска равнялась диску, чужих строк не было, «принято» стояло за контейнерами. Он просто не шёл, и молчал об этом.

Отсюда правило, применимое ко всей карте: **у каждой стадии, кроме вопроса «не соврала ли», должен быть вопрос «не встала ли молча»** — и ответ на второй нельзя вывести из первого. Практическая форма: любой уход при непустой очереди обязан называть причину, а любое ожидание — свой конец.

Второй урок — из §10: **находка, названная симптомом, работает как диагноз.** Рапорт А-37.1 был честен и точен в наблюдении («после каждой вехи нужен внешний ход») и увёл работу к планировщику, тогда как ломался тариф. Правило: где механизм не вскрыт, писать «симптом, причина не найдена» — это дешевле одной сессии.
