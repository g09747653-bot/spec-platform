/**
 * `specs` — spec file domain model, section schema, revisions, diffing, export.
 *
 * Owns `section-schema.ts`, the single source of truth for required headings (constitution P3, D-16).
 *
 * May import: repository interfaces, capability interfaces.
 * Must not import: any other module.
 */
export const MODULE_ID = 'specs';
