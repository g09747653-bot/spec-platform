/**
 * The allowed spelling of the fixture next door (task 124).
 *
 * A gate that only proves it rejects is half a gate: this one proves the token utilities — which
 * contain the words `danger` and `surface` and are the thing components are supposed to write —
 * are not swept up by the same rule.
 */
export const errorTextClass = 'text-caption text-danger-ink bg-danger-soft border-border-subtle';
