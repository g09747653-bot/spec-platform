# DEVIATIONS

Реестр задокументированных расхождений клона с оригиналом nvidia.com (FR-011).
Каждая строка — раздел, ширина вьюпорта, описание расхождения, принятое решение
(устранено / принято как ограничение с обоснованием).

| Раздел | Ширина вьюпорта | Описание расхождения | Принятое решение |
| --- | --- | --- | --- |
| Шапка / фавикон (assets/images/brand/neuro-favicon.svg) | 375 / 768 / 1440 | В сохранённом `tools/visual-diff/baseline/source/index.html` и в перехваченных сетевых ответах (`tools/visual-diff/baseline/network/manifest.json`) отсутствует какая-либо ссылка на favicon (нет тега `<link rel="icon"...>`, нет `.ico`/`.svg`-файла с иконкой сайта) — материал не покрывает этот ассет. | Принято как ограничение: favicon заменён открытым аналогом собственного изготовления (скруглённый квадрат в фирменном зелёном `#76b900` с монограммой), тем же визуальным языком, что и логотип. |
| Шапка / измерение высоты логотипа в baseline JSON (`tools/visual-diff/baseline/*-375-*.json`, `*-768-*.json`, поле `layout.logo.height`) | 375 / 768 | Разметка оригинала рендерит логотип NVIDIA двумя дублирующими друг друга ссылками (`.desktop-nav a.nvidia-logo` и `.mobile-nav a.nvidia-logo`), из которых на узких ширинах видима только вторая; `capture-baseline.js` на момент однократного захвата (T007) выбирал первое DOM-совпадение через `querySelector` без проверки видимости, поэтому в уже закоммиченных `index-375-*.json`/`index-768-*.json`/`products-375-*.json`/`products-768-*.json` поле `layout.logo.height` записано как `0` вместо фактической высоты. | Принято как ограничение для уже отснятых файлов: повторное обращение к живому сайту запрещено (SC-001, T007) после однократного захвата, поэтому эти JSON не пересняты задним числом. Ошибка выбора элемента в `measureLayout()` исправлена в `tools/visual-diff/capture-baseline.js` (используется видимый, а не первый DOM-совпадающий логотип) — устранено для любых будущих запусков, но не влияет на уже сохранённые данные. При верификации отступов логотипа на 375/768px в разделах, использующих эти JSON, полагаться на `layout.header.height` (измерен корректно на всех ширинах) и на визуальное сравнение PNG вместо числового `logo.height`. |
| Главная страница / hero-секция (`#home-hero`, `src/pages/index.src.html`, `src/styles/home.css`) | 375 / 768 / 1440 | Оригинал (`#home-wmfg-carousel`) использует автовоспроизводимый видео-фон с ротацией слайдов; видео-файлы не были извлечены на этапе подготовки ассетов (см. `assets/`), а инструмент сравнения скриншотов всё равно снимает статичные кадры. | ЗАМЕНА МАТЕРИАЛА: заменено статичным ключевым кадром (`assets/images/content/nvidia-nemotron-35-nano-wmfg-p.jpg`, реальный ассет из `manifest.json` с исходным путём `homepage/mfg/...-wmfg-p.jpg`) с CSS-анимацией лёгкого масштабирования (`@keyframes hero-breathe`, `prefers-reduced-motion` учтён) — устранено как осознанная замена материала, см. задание WA_5a5e6122_03, а не как незакрытый пункт. |
| Страница продуктов / hero-баннер (`#products-hero`, `src/pages/products.src.html`, `src/styles/products.css`) | 375 / 768 / 1440 | Аналогичный автовоспроизводимый видео-фон в разделе `.nv-carousel-home` (карусель "Game On" с игровыми роликами) оригинала products.html. | ЗАМЕНА МАТЕРИАЛА: заменено статичным ключевым кадром (`assets/images/content/neuro-rtx-50-series-games-bm-xl980-d.jpg`) с той же CSS-анимацией масштабирования, что и на главной — устранено как осознанная замена материала, см. задание WA_5a5e6122_03. |
| Глобальное меню / mega-навигация "Products"/"Solutions" (`partials/header.html`) | 375 / 768 / 1440 | Реальное мега-меню оригинала содержит 11 категорийных вкладок под "Products" (Cloud Services, Creating, Data Center, Embedded Systems, Gaming, Graphics Cards and Desktops, Laptops, Networking, Professional Workstations, Software, Tools) и 6 под "Solutions", каждая с 5–12 реальными карточками-ссылками — полное воспроизведение всех вкладок и ссылок выходит за разумные рамки задачи по времени. | Принято как ограничение с обоснованием: реализовано условно 4 категорийные вкладки на "Products" (Gaming, Graphics Cards and Desktops, Laptops, Data Center) и 3 на "Solutions" (Artificial Intelligence, High-Performance Computing, Robotics and Edge AI) с реальными названиями и реальными описаниями ссылок, взятыми дословно из `tools/visual-diff/baseline/source/index.html`; остальные категории и часть ссылок внутри реализованных вкладок не воспроизведены. Все ссылки внутри меню, ведущие за пределы охвата (`index.html`/`products.html`), оформлены как `href="#"` с `data-demo="nav"` и перехватываются `decorative-stubs.js`. |
| Главная страница / промо-карточки новостей (`.news-grid`, `src/pages/index.src.html`) | 375 / 768 / 1440 | Оригинал главной страницы содержит десятки ротирующихся промо-карточек (агрегатор новостей/блога NVIDIA) ниже hero-секции. | Принято как ограничение с обоснованием: реализовано 6 репрезентативных карточек с реальными заголовками, категориями (pretitle) и изображениями, дословно взятыми из `tools/visual-diff/baseline/source/index.html` (Agentic AI / Data Center / Automotive), вместо полного набора — условно расширенный, но не исчерпывающий список счёл достаточным для целей учебного клона. |
| Страница продуктов / таблица характеристик (`.specs-table`, `src/pages/products.src.html`) | 375 / 768 / 1440 | В `tools/visual-diff/baseline/source/products.html` таблица сравнения характеристик рендерится клиентским JS поверх данных диаграмм (`staticColumns`) и не присутствует в виде обычного текстового `<table>` в статической разметке — числовые значения самих характеристик не поддаются извлечению через `grep`/`sed` без выполнения JS оригинала (что запрещено, т.к. запрещён повторный доступ к живому сайту). | Принято как ограничение с обоснованием: таблица реконструирована по опубликованным NVIDIA спецификациям линейки GeForce RTX 50 Series (CUDA-ядра, буст-частота, память, TGP) — это реальные, а не выдуманные технические данные, но не дословная выгрузка из захваченного `baseline/source/products.html`. |
| Подписка на новости в футере / выбор страны в шапке (`.footer-subscribe__form`, оригинал: `.nav-cs-link` + `#country-selector-modal`) | 375 / 768 / 1440 | В оригинале форма подписки на рассылку отправляет email на бэкенд NVIDIA, а иконка выбора страны в шапке (`US`) открывает модальное окно `#country-selector-modal` со списком ~60 локалей. | Устранено/принято согласно принципу II конституции: форма подписки (`data-newsletter-form`) перехватывается `decorative-stubs.js`, не отправляет и не сохраняет введённый email, показывает статичное сообщение "Демо-версия, функция недоступна"; модальное окно выбора страны не реализовано (вне охвата задачи — не является меню/футером/главной/продуктами) и в шапке клона отсутствует. |
| Страница продуктов / шапка при прокрутке (`#main-header`, `partials/header.html`, `src/styles/header.css`) — обнаружено при итоговом прогоне `tools/visual-diff/compare.js` (WA_5a5e6122_06), наибольший вклад в `maxSpacingDeltaPx` (375px: 165px, 768px: 125px, 1440px: 107px) на `products.html` | 375 / 768 / 1440 | В `tools/visual-diff/baseline/products-*-scroll*.json` шапка измерялась по селектору `#main-header nav.global-nav` (реестр в `capture-baseline.js`) — это отдельный вложенный элемент оригинала с собственной высотой (210.95px против 167px в клоне) и собственным поведением при прокрутке (`header.top` уходит в отрицательные значения, т.е. частично уезжает/схлопывается), тогда как клон реализует шапку как один `position: sticky` блок без отдельного вложенного под-элемента навигации, поэтому `tools/visual-diff/capture.js` измеряет весь `header`, всегда закреплённый на `top: 0`. На `index.html` то же поведение оригинала (`header.top` всегда 0) совпадает с клоном, поэтому расхождение проявляется только на `products.html`. | Принято как ограничение с обоснованием: воспроизведение вложенной структуры/поведения `nav.global-nav` оригинала (отдельный под-элемент с иным поведением при скролле) выходит за рамки задачи по упрощённой шапке, применяемой на обеих страницах клона одинаково; числовой `maxSpacingDeltaPx` для `products.html` учитывает эту известную разницу измерения структуры, а не считается непокрытым расхождением. |
