# Кадры-разницы в репозиторий не едут.
#
# Это производные от 87 эталонных кадров чужого живого сайта: попиксельная разница
# восстанавливает изрядную часть исходного изображения. Замер судится ЧИСЛАМИ —
# measure.json остаётся под версией целиком, включая каждый кадр отдельной строкой;
# сами PNG лежат на машине заказчика, где им и место.
diff/
*/diff/
