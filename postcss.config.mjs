/**
 * Tailwind CSS v4 is a PostCSS plugin; its configuration lives in CSS
 * (`src/app/globals.css`, `@theme`) rather than in a JavaScript config file.
 */
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
